# Changelog — Tycoon v36

## 2026-09-21 — Импорт файла v36.7 «Stage Road Reset»

- Импортирован присланный пользователем файл (30MB, изначально 15421 строка) —
  продолжение линии v31 на 5 стадий дальше: та же экономика/компания/город, плюс уже
  готовая расширенная дорожная сеть, участки земли и гражданский трафик.
- Вынесены вендоры/ассеты по уже отработанной технике (точные, проверенные через grep
  номера строк, без нечёткого поиска): boot-error хендлер оставлен как есть
  (797-820), three.js r128 (821-824) и OrbitControls (825-1885) заменены на
  `<script src>`, `EMBEDDED_ASSETS` (строка 1889) указывает на уже существующие
  `assets_v31/*` — все 10 текстур побайтово идентичны (sha256), новых файлов не
  добавлено. `tycoon-v36.html` переиспользует `vendor_v31/three_r128/`.
- Проверено скриншот-сравнением оригинала и вырезанной версии — идентичный рендер,
  идентичная (уже существующая в исходнике) ошибка `createIconSpriteV36 is not
  defined` в `buildWorldNavPadV362` — подтверждает, что вырезка ничего не сломала.
- Коммит `919a3ca`. Документы (`TYCOON_V36_PLAN.md` + этот файл) — следующим коммитом.
- Запрос пользователя на этот заход: убрать баги дорог, нормальная физика движения,
  улицы как в реальном городе, всё должно выглядеть шикарно. Работа над этим —
  в следующих коммитах.

## 2026-09-21 — Аудит и фикс: дороги, физика движения, столкновения зданий

Прямой запрос пользователя (приоритет выше общего roadmap): «сделать так, чтобы город
был похож на настоящий — убрать баги дорог, нормальная физика движения, улицы как в
реальном городе, всё должно выглядеть шикарно». Полный аудит-и-фикс проход по всем
пунктам ниже, с проверкой через Playwright (скриншоты + численные проверки геометрии),
не только на глаз.

### 1. Исправлен подтверждённый баг `createIconSpriteV36 is not defined`

- Причина: `buildWorldNavPadV362` (мировые нав-иконки — Компания/Город/Команда/Автопарк/
  База/Проекты/Тендеры/Операции/Мета/Бонусы) вызывал `createIconSpriteV36(cfg.icon)`,
  ожидая текстуру для своего `SpriteMaterial`, но такой функции в файле не было вообще —
  подтверждено grep'ом и живым `ReferenceError` на каждой загрузке страницы. Ломало
  создание всех 10 мировых нав-панелей целиком (весь `createWorldNavPadsV362` падал в
  catch).
- Фикс: существующая `makeIconSprite(symbol, size)` (линейные иконки вместо эмодзи)
  разбита на `makeIconCanvasV36(symbol)` (сама отрисовка на канвасе) + тонкие обёртки:
  `makeIconSprite` возвращает готовый `THREE.Sprite` (как раньше, ничего не изменилось
  для существующих вызовов), новая `createIconSpriteV36(symbol)` возвращает just
  `THREE.CanvasTexture`, как и ожидал вызывающий код. Добавлены 8 новых веток отрисовки
  в `makeIconCanvasV36` для символов, которых не было в исходном свитче
  (☰ 🌆 👷 📋 🛰️ 🧬 🎁 — иконки мирового нав-меню), чтобы все 10 панелей получили
  осмысленную иконку, а не общий fallback-кружок с крестиком.
- Проверено Playwright: 0 ошибок консоли на загрузке (раньше — гарантированный
  `ReferenceError` на каждой загрузке), скриншот подтверждает иконку на нав-панели
  (`hasIconMap: true` для всех 10 панелей).

### 2. Реальный баг: мини-дома соседств визуально залезали в здания района

Численный аудит (grid-sample по всей карте каждые 2.5 юнита, 3249 точек +
покруглосный `THREE.Box3`-чек всех реальных мешей друг против друга — не просто
дистанция между центрами) нашёл, что общая сеть дорог/перекрёстков **уже в хорошем
состоянии** (нет грубых пересечений, нет z-fighting разметки, ширина дорог
консистентна, съезды к районам аккуратные) — но нашёл настоящий баг не в дорогах, а в
плотности застройки: декоративные мини-дома соседства (`refreshNeighborhoodWorld`,
файл создаёт их вокруг жилых кварталов) проверяли клиренс до других зданий района
(поликлиника/школа/полиция/коммерция/пожарная часть) с фиксированным радиусом **2.3**
от центра — но эти здания имеют реальный радиус footprint до ~1.62 (а пожарная часть,
архетип `warehouse`, размером `baseSize*1.92 × baseSize*1.32` — полудиагональ ~2.97!),
плюс сам домик радиусом 0.92. Итог: реальный клиренс уходил в минус, дом визуально
залезал в стену пожарной части/коммерческого здания/школы (подтверждено скриншотом и
`Box3.intersectsBox` — пересечение объёмом до 0.82×1.44×1.74 юнита).
- Фикс (`refreshNeighborhoodWorld`, функция дорожной/застроечной плотности): клиренс
  поднят с 2.3 до 4.3 (перекрывает полудиагональ самого широкого архетипа + радиус
  домика + запас); добавлена ранее отсутствовавшая проверка домика против уже
  размещённых домиков того же соседства (не были защищены от наложения друг на друга).
- Проверено: полный прогон "построить всё" (все 4 района ур.3, всё жильё/клиники/
  полиция/школы/коммерция/парки/пожарные части/участки земли застроены) +
  `THREE.Box3` попарная проверка всех крупных + мелких мешей во всех 4 районах —
  **0 пересечений** (было 1 подтверждённое, детектор находил его стабильно). Визуально
  подтверждено скриншотами до/после (домик у пожарной части в Suburb раньше касался
  стены, теперь стоит с видимым зазором).
- Единственная оставшаяся находка: статичный коллайдер лесопилки (`SAWMILL_POS`,
  существует с версий до v31, не трогалась в этой сессии) технически перекрывается с
  дорогой на **3 см** (радиус клиренса 2.15 против фактических 2.12) — это допуск
  коллизии игрока, не геометрии меша; визуально незаметно на любом разумном расстоянии
  камеры. Оставлено как есть — вне зоны прямого запроса, не годами дорожной сети этой
  сессии, риск правки существующего давно стабильного объекта не оправдан для 3 см.

### 3. Физика движения игрока и камера — проверено, изменений не потребовалось

Прочитан `updatePlayer` (движение) и вся follow-cam логика. Система уже зрелая:
инерционное ускорение/торможение (`PLAYER_ACCEL`/`PLAYER_BRAKE` с экспоненциальным
lerp, а не мгновенная смена скорости), суб-шаги коллизии (`PLAYER_PHYSICS_STEP`,
защита от туннелирования сквозь тонкие объекты на высокой скорости), камера следует
за игроком с обходом препятствий через raycast (`cameraRaycaster`) и плавным
восстановлением дистанции, с grace-периодом после ручного вращения камеры, чтобы игра
не "боролась" с игроком. Это уже решает в точности тот класс проблем, который был
найден и исправлен в v19 (рывок камеры от джойстика).
- Проверено симуляцией: прогнан `updatePlayer` вручную на фиксированных 60 fps через
  последовательность резких вводов джойстика (внезапный полный толчок вперёд → резкий
  свайп в страйф → отпускание → резкий диагональный толчок в другую сторону).
  Максимальная дельта позиции камеры за один кадр (1/60с) — 0.36 юнита, и это часть
  плавного многокадрового экспоненциального затухания (реакция на резкую смену
  направления игрока), а не одноразовый скачок/телепорт. Признаков рывка/рассинхрона
  камеры не найдено — тот класс бага, который чинили в v19, в этой линии уже не
  воспроизводится.
- Решение: изменений в этой части не вносилось — система уже соответствует запросу
  («нормальная физика движения»), рисковать стабильной, тщательно настроенной системой
  без найденного бага не стал (see "don't rewrite" non-negotiable).

### 4. Визуальный проход по дорогам — состояние уже хорошее, точечных правок не потребовалось

Скриншоты до/после (top-down всей карты, top-down и наезд по каждому из 4 районов,
вид на уровне вождения по главной дороге и у площади, крупный план перекрёстка)
подтвердили: тёмный асфальт (`ROAD_STYLE_V353`) консистентен по всей сети, разметка
(прерывистая жёлтая осевая + сплошные белые кромки) читается чётко на дистанции
вождения, тротуары/поребрики визуально отделены от асфальта, перекрёстки и Т-образные
стыки главной дороги с районными артериями — без острых изломов и без видимых щелей.
Ранее в этом проекте уже чинили мигание разметки от z-fighting (разнесение по Y) —
в текущей сети (`addRoadSegmentPremiumV353`) разметка и асфальт уже разнесены по Y
(`0.028` тело / `0.067` поребрик / `0.075` осевая / `0.074` кромка), новых случаев
мигания не найдено.

### Осознанно не тронуто / оставлено на будущий заход

- 3-сантиметровое пересечение коллайдера лесопилки с дорогой (см. пункт 2) — не
  относится к дорожной сети этой сессии, эффект неразличим визуально.
- Полноценная переработка дорожной сети (перекрёстки со светофорами, разные типы
  покрытия, дорожные знаки) не делалась — не соответствует принципу "точечные правки,
  не переписывать сеть с нуля"; текущая сеть уже оценена как качественная по итогам
  скриншот-аудита.
- Физика движения — читалась и тестировалась, но не переписывалась: реального бага не
  найдено, а система уже достаточно "весомая" (инерция, суб-шаги, обход препятствий)
  для лёгкой мобильной игры; дальнейшее усложнение было бы избыточной инженерией без
  подтверждённой проблемы.
- `saveVersion` не поднимался — изменения (fix иконок, клиренс мини-домов) не трогают
  форму сохранения ни одним полем; проверено загрузкой минимального сейва старого
  формата (`saveVersion: 1`) — грузится и мигрирует без ошибок.
- Проверено (Playwright): чистая загрузка + WASD-заезд без ошибок консоли; все панели
  (Город/Команда/Автопарк/База/Проекты/Тендеры/Операции/Мета/Бонусы/Компания)
  открываются и рендерятся без ошибок после полной "стройки" города; старый формат
  сейва грузится корректно.

## 2026-09-21 — Дороги: текстура асфальта и яркая разметка для новой дорожной сети

Прямая жалоба пользователя после предыдущего аудита: «дороги какие-то скучные, не
понятно что это дороги». Причина найдена быстро: у оригинальной дороги-спирали
(`pathMaterial`) уже давно есть текстура асфальта с bump-картой и яркая жёлтая
разметка с лёгким свечением (`lineMaterial`) — а у новой дорожной сети (`ROAD_STYLE_V353`,
артериальные/районные/служебные дороги из «Stage Road Reset») асфальт был плоским
однотонным `MeshStandardMaterial` без карты вообще, разметка — полупрозрачным тонким
`MeshBasicMaterial` низкой насыщенности. Отсюда и ощущение «не похоже на дорогу»: новая,
более развитая сеть дорог визуально была куда более скучной, чем старая.

Правки (`ROAD_STYLE_V353` и `addRoadSegmentPremiumV353`):
- `asphalt`/`asphaltDark` теперь используют тот же `roadTexture` (уже загруженная
  текстура асфальта, без новых ассетов) + `getDetailBump('asphalt')`, что и у
  оригинальной дороги — материал переиспользуется, не клонируется на каждый сегмент,
  расход памяти/дроуколлов не растёт.
- `lane` (осевая разметка) — с полупрозрачного `MeshBasicMaterial` на
  `MeshStandardMaterial` с лёгким emissive-свечением (0xf5d77d, emissive 0x3d310b) —
  тот же приём, что уже хорошо смотрелся на старой дороге.
- `edge` (кромочная разметка) — с полупрозрачного на непрозрачный светлый
  `MeshStandardMaterial`, чётче читается на расстоянии.
- Бордюр (`curbWidth`) расширен с 0.09 до 0.14, осевая разметка (`dash`) — с 0.07 до
  0.10, кромка (`edge`) — с 0.035 до 0.05 — пропорции подтянуты к оригинальной дороге.
- Y-смещения (высота слоёв асфальт/бордюр/разметка) не трогались — они уже были
  разнесены корректно (z-fighting не было, это подтвердил предыдущий аудит), риск
  регрессии не оправдан.

Проверено Playwright: 0 ошибок консоли; скриншоты сверху, с уровня вождения и вблизи
районной дороги подтверждают выраженную тёмную текстурированную полосу асфальта,
яркую жёлтую осевую разметку и белые кромки — сеть дорог теперь читается как дороги на
любом проверенном ракурсе, а не как серые плоские полосы.

## 2026-09-21 — Дороги: разгрузка «солнечного луча» стадийных дорог от хаба (клубок дорог)

Прямая жалоба пользователя сразу после предыдущей записи: «щас какая-то помойка из
дорог» — на широком скриншоте полностью отстроенного города (все 4 района ур.3, вся
инфраструктура, все участки земли) видно десятки тонких прямых отростков под
произвольными углами, пересекающих друг друга и артериальную сетку без общих узлов.
Материалы из предыдущей правки тут ни при чём напрямую (не трогались) — проблема
структурная, но именно возросшая яркость сделала её куда заметнее.

Найдено (grep по актуальным, ПОСЛЕДНИМ переопределениям функций — файл переприсваивает
дорожные функции послойно через `v36.2`→`v36.5`→`v36.6`→`v36.7`, поэтому актуален только
самый нижний по файлу вариант каждой): подъездные дороги района (`refreshDistrictAccessRoads`,
v36.6) и участки земли (`createLandPlotMesh`, v36.6) уже были приведены к аккуратному
ортогональному примыканию предыдущими проходами — на уровне района клубка не нашлось.
Настоящий источник — `makeStageRoadGroupV367`/`refreshStageRoadsV367` (v36.7 «Stage Road
Reset»): 10 прямых «спиков» от центрального хаба (0,0,0) до каждого здания основной линии
прогресса (`STAGES`), позиции которых заданы спиралью (`stagePosition`: угол = i·0.95 рад,
радиус = 7 + i·4.6) — т.е. 10 линий под произвольными несогласованными углами, все с
одинаковым ярким «уличным» стилем (сплошная пунктирная осевая), расходящиеся из одной
точки и режущие прямоугольную артериальную сетку (`masterRoadSegmentsV362`, ~±18×±16)
далеко за её пределами — классический эффект «солнечных лучей поверх решётки». Заодно
обнаружилось, что `v36.5` полностью обнулил `addRoadNodeCapV353`
(`addRoadNodeCapV353=function(){};`) — во всей текущей системе не осталось ни одной
визуальной заглушки на стыках дорог.

Правки (только визуальный слой; `stageRoadEndpointV367`/`spokeProjectV367` — от которых
зависят `buildRoadRoute`/`projectToRoad`/логистика грузовиков — не тронуты, а
`distToNearestRoadSegment` вообще работает по отдельному, не связанному с этим
`pathPoints`, так что клиренс-проверки не затронуты в принципе):
- `makeStageRoadGroupV367`: спики с конечной точкой внутри охвата артериальной сетки
  (радиус ≤ 24.5, чуть больше её диагонали угла ~24.08) остаются полноценной «улицей».
  Спики за пределами сетки (дальние, более поздние стадии — именно те, что резали решётку
  и кольцевую дорогу) понижены до тонкого немаркированного «проезда» (`markings:false`,
  `edgeMarks:true`, ширина 0.86 вместо 1.38–1.48) — тот же язык, что уже используется для
  подъездов района и служебного графа. Количество линий и их углы не менялись (роутинг не
  тронут) — снижена именно визуальная «конкуренция за внимание» между спиками, из-за
  которой сеть выглядела как клубок после того как материалы стали ярче.
- `refreshArterialRoads`: добавлены 8 маленьких тёмных асфальтовых заглушек
  (`addMasterGridJointCapsV368`) в реальных узлах артериальной сетки (4 угла + 4
  T-образных стыка с центральным крестом) — чисто декоративные компланарные диски вне
  `ROAD_NODES`/коллизий, сглаживающие места, где сегменты раньше протыкали друг друга.

Осознанно оставлено на будущий заход:
- Спики всё ещё идут по прямой от хаба к каждому зданию, а не сначала по сетке с коротким
  финальным подъездом (вариант «привязка к общим узлам») — самый чистый структурный
  вариант, но требует либо менять расчёт `stageRoadEndpointV367`/маршрут грузовиков, либо
  вводить отдельный визуальный маршрут, расходящийся с логическим — риск/сложность выше,
  чем оправдано сейчас; после понижения яркости внешних спиков результат уже читается как
  «решётка + немного тонких подъездов», а не клубок.
- Служебный граф депо (юго-западный угол карты, рядом с хабом) не трогался — самостоятельная
  стабильная логистическая система, вклад в общий визуальный шум небольшой (уже тонкий
  некрашеный стиль), риск задеть `buildRoadRoute`/доставки не оправдан.
- `saveVersion` не поднимался — правка чисто визуальная, формат сохранения не тронут;
  старый сейв (`saveVersion: 1`) проверен — грузится без ошибок.

Проверено Playwright (полная стройка: деньги/ресурсы + `upgradeCityDistrict`×3 на район +
жильё/поликлиника/полиция/школа/коммерция/парк/пожарная на район + все участки земли
куплены/зонированы/принудительно достроены, все 4 района ур.3): 0 ошибок консоли на
загрузке и после полной стройки; топ-даун скриншоты (широкий вид карты + приближенный вид
хаба) до/после подтверждают явное снижение визуального шума; численная grid-сэмпл проверка
`distToNearestRoadSegment` (625 точек по всей карте) — идентичные `min`/`max`/`avg` до и
после стройки, клиренс не задет; гражданские машины продолжают двигаться (позиции меняются
между замерами через 2с); панели Город/Системы/Автопарк/База/Операции/Проекты открываются
и рендерятся без ошибок на полностью отстроенном городе.

## 2026-09-21 — Пешеходные дорожки: отдельная сеть для резидентов + маршрутизация вместо прямой

Прямая жалоба пользователя (дословно, перевод с русского): «Что за фигню ты сделал —
жители так не ходят по дорогам, это для машин. Напиши спецификацию, какими должны быть
дороги и какими должны быть пути для жителей — они не должны быть идеально прямыми, это
дёшево [cringe]».

Причина (найдена grep'ом, не заново): пешеходы (`livingCityRuntime.residents`,
`createResidentNPC`) двигались строго по прямой к `r.target` (`chooseResidentTarget` задаёт
цель; цикл `updateLivingCity` считал вектор до цели и шёл по нему — без путевых точек, без
понятия «тротуар vs. проезжая часть»), поэтому по прямой срезали через ту же проезжую часть,
которую три предыдущих захода этой сессии как раз приводили в порядок (материалы асфальта,
разметка, разгрузка «клубка» дорог). Сами дороги (`ROAD_STYLE_V353`/
`addRoadSegmentPremiumV353`/артериальная сетка) в этом заходе не трогались вообще.

Спецификация пользователя (реализована как есть): дороги — только для транспорта, по
унифицированной сетке, со стандартной шириной, асфальтом, разметкой и поребриком —
без изменений. Пешеходные дорожки — отдельный слой геометрии, не дублирующий форму дороги
1:1; мягкие естественные изгибы из нескольких коротких сегментов со случайным смещением;
другой материал (грунт/гравий, без разметки); соединяют дом/остановку/площадь/парк коротким
«естественным» маршрутом, не копируя маршрут дороги; резиденты реально идут по ним к цели
вместо прямой линии через что угодно, включая дороги.

Правки (новый блок «v36.9» в конце файла, перед `INIT` — ничего из v36.2-v36.8 не
переопределялось повторно, кроме одной обёртки `refreshV34CityWorld`, тем же паттерном
послойного переопределения, что уже используется в файле):

- **Сеть дорожек** (`buildFootpathSpineV369`/`refreshResidentFootpathsV369`): для каждого
  разблокированного района с уровнем >0 собираются реальные пешеходные якоря (вход(ы) жилья,
  торговый/рабочий якорь, парк/школа если построены, транзитный якорь, площадь района),
  сортируются по углу вокруг центра района и соединяются в один замкнутый контур — каждое
  ребро превращается в 2-4-точечную ломаную с небольшим перпендикулярным смещением на
  детерминированном хэше (не `Math.random()`, чтобы контур не «прыгал» при каждом ребилде);
  из двух кандидатов смещения выбирается тот, что дальше от реальной дорожной сети —
  `distToNearestRoadNetworkV369` (новая функция; в отличие от `distToNearestRoadSegment`,
  которая смотрит только на старый `pathPoints`, эта собирает геометрию из уже существующих
  `masterRoadSegmentsV362`/`districtArterialPath`/`districtBlockDimsV362`, ничего не
  пересчитывая заново). Пересчитывается по тем же триггерам, что `refreshDistrictAccessRoads`/
  `refreshLandPlotWorld` (через `refreshV34CityWorld`), не каждый кадр.
- **Визуальный стиль** (`FOOTPATH_STYLE_V369`): переиспользована уже загруженная текстура
  грунта + бамп (`makeDirtTexture()`/`getDetailBump('dirt')`, тот же приём, что у
  `innerPlaza`), тон более тусклый/изношенный; ширина 0.40-0.62 (у самой узкой дороги — от
  0.86), без осевой разметки, без поребрика. Новых ассетов не добавлено.
- **Маршрутизация резидентов** (правка только внутри `updateLivingCity`, ~10652-10685;
  `chooseResidentTarget` не тронут — перехватывается потребитель `r.target`, а не сама
  функция выбора цели, которая уже переопределялась один раз в v36.4).

  Первая версия (не прижилась, см. ниже почему) вела резидента по ОБЩЕМУ районному контуру
  (`sampleClosedPathV365` по дуге до проекции цели на контур). Числовая проверка — детерминированный
  прогон всех осмысленных пар «дом/работа/магазин/транзит/площадь» во всех 4 районах (70 пар,
  без случайности, одна и та же пара стартовой/целевой точки для «было»/«стало») — показала, что
  контур ЧАЩЕ делал хуже, чем лучше (32 хуже / 14 лучше / 24 без изменений, среднее
  непрерывное «время у дороги» выросло с 5.17 до 5.53 юнита): контур нанизывает ВСЕ якоря района
  по углу вокруг центра, а собственное кольцо улиц района лежит примерно на том же радиусе, что и
  сами якоря (зданиям нужен проезд к улице) — так что контур физически идёт почти параллельно
  кольцевой улице большую часть пути, и короткая по прямой пара точек могла уйти в большой обход
  через посторонние узлы контура.

  Итоговая версия строит **отдельный, короткий изогнутый обход для каждой конкретной поездки**
  (`computeResidentFootRouteV369`), напрямую между текущей позицией резидента и `r.target`, тем же
  хелпером `footpathCurvedSegmentV369`, что строит визуальную сеть (тот же визуальный «почерк»,
  но не по общему контуру): для поездок короче 3.5 юнита — прямая линия, как раньше (короткий
  рывок до двери, о котором просит задача). Иначе строится 2-4-точечная ломаная с изгибом,
  который `footpathCurvedSegmentV369` перебором (магнитуды 1.1/1.8/2.6/3.4 в обе стороны) старается
  увести на `FOOTPATH_ROAD_CLEARANCE_V369`=1.7 от дорожной сети; если даже лучший найденный вариант
  не набрал клиренс ≥1.35 (например, сама цель — «площадь», которая физически стоит на пересечении
  двух внутренних улиц района, как настоящая площадь у перекрёстка в реальном городе — обойти
  такое бессмысленно, объезд только удлиняет путь без выгоды) или обход вышел ощутимо длиннее
  прямой (>1.6×+2), резидент идёт по прямой — безопасный фолбэк, не хуже прежнего поведения.
  Резидент проходит эти точки по очереди (тот же лёгкий паттерн, что уже используют
  гражданские машины для маршрута, `moveWaypointCityVehicle`/`c.routeIndex`, только без полос
  и пауз на поворотах). O(1) на резидента на кадр; сам изгиб считается один раз при смене цели,
  не каждый кадр.

Проверено Playwright:
- Полная стройка (те же шаги, что в предыдущих записях; участки земли зонированы напрямую через
  `districtLandState` — см. находку ниже): 0 ошибок консоли на загрузке и после стройки.
- Скриншоты с позиции резидента у реального сегмента дорожки (`cityWorldRuntime.footpathSpines`)
  в районах Suburb и Business подтверждают явно отличимую тонкую светлую грунтовую дорожку без
  разметки рядом с тёмным асфальтом с белой разметкой — не клон дороги.
- **Детерминированная проверка маршрутизации** (70 пар якорей «дом/работа/магазин/транзит/площадь»
  во всех 4 районах, одна и та же пара точек для «было»/«стало», не случайные выборки — первая
  версия с общим контуром была проверена именно случайными выборками и давала обманчиво
  оптимистичные числа, пока эта проверка не показала, что «было»/«стало» на самом деле сравнивали
  РАЗНЫЕ случайные поездки): 18 пар — заметно лучше, 5 — немного хуже (все — поездки, где
  начало/конец у самой «площади», на пересечении внутренних улиц — вариант, который любой обход
  физически не может улучшить, сама точка на перекрёстке), 47 — без изменений (сработал безопасный
  фолбэк на прямую). Среднее по всем 70 парам «самой длинной непрерывной прогулки в пределах
  ширины дороги»: 5.17 → 4.18 юнита (−19%). Отдельный случайный прогон (8 резидентов, домашний
  анchor → магазин района, одинаковые стартовые точки для «было»/«стало»): 0.38 → 0.13 юнита
  (−65%). Резиденты по-прежнему иногда пересекают дорогу (короткое пересечение перекрёстка — это
  нормально и ожидаемо), но заметно реже идут ВДОЛЬ неё подолгу.
- Гражданский трафик (`trafficAuditSnapshot`, машины/автобус/скорая/полиция/пожарная) без
  изменений в маршрутах; панели Город/Системы рендерятся без ошибок.

**Побочная находка (не трогалась, вне рамок задачи):** при написании теста обнаружен
настоящий, не связанный с этим заходом баг в `buyLandPlot(id)` — функция берёт ссылку на
`plot` из `districtLandState(id)`, потом вызывает `landPlotPurchaseCost(id)` (которая сама
вызывает `districtLandState(id)` ещё раз, а та при каждом вызове безусловно пересоздаёт
`st.plots` как новые объекты — нужно для санитизации старых сейвов) и только потом делает
`plot.owned=true` — к этому моменту ссылка уже отсоединена от реального массива, и покупка
участка молча теряется. `zoneLandPlot` этой проблемы избегает (там `p` перечитывается заново
прямо перед мутацией, без вызовов между ними). Баг застройки земли (Stage A/B), не
дорог/резидентов — не входит в объём этой задачи, не трогался.

Осознанно оставлено на будущее: это не полноценный navmesh/A* — каждая поездка получает свой
изгиб между двумя конкретными точками, а не маршрут по общему графу; для длинных поездок с
несколькими препятствиями подряд один изгиб может не обойти всё. Поездки, где сама целевая
точка стоит у перекрёстка (площадь района), не улучшаются обходом и честно падают на прямую —
описано в проверке выше. Пересечения резидентов с дорогой не исключены полностью и не должны
быть (сам пользователь считает нормальным, что дорожка иногда пересекает улицу) — цель была
именно сократить случаи длинного движения ВДОЛЬ дороги, а не убрать пересечения совсем.
Визуальная сеть дорожек (`cityWorldRuntime.footpathSpines`, замкнутый контур на район) и
маршрутизация резидентов теперь используют один и тот же изгибающий хелпер
(`footpathCurvedSegmentV369`), но не буквально один и тот же путь — резидент не обязан идти
именно по нарисованным на земле сегментам, только в похожем стиле; свести их в одно и то же в
будущем можно, но не требовалось по духу задачи («либо сеть путевых точек, либо максимально
близкое приближение»). Баг `buyLandPlot` не исправлен (отдельная задача). `saveVersion` не
поднимался — резиденты и пешеходные дорожки не сериализуются в `save()` вообще (подтверждено
grep'ом), формат сохранения не тронут.

## 2026-09-21 — Исправлен баг `buyLandPlot`: покупка участка земли молча терялась

Отдельная задача, отложенная в прошлой итерации. Причина (точно как было описано): `buyLandPlot`
брал ссылку `plot=st.plots[index]` из `districtLandState(id)`, затем вызывал
`landPlotPurchaseCost(id)` — а та внутри себя ещё раз вызывает `districtLandState(id)`, которая
**безусловно** пересобирает `st.plots` в новый массив новых объектов на каждый вызов (нужно для
санитизации старых сейвов). К моменту `plot.owned=true` ссылка `plot` уже указывала на отсоединённый
от `cityState.land[id].plots` объект — деньги списывались, тост показывался, но сам участок в
реальном состоянии оставался непроданным. `zoneLandPlot` этой ловушки уже избегал (перечитывает `p`
заново после любого вызова, который может задеть `districtLandState`) — `buyLandPlot` был
единственным местом с этим багом.

Фикс: `plot` теперь читается заново из свежего `districtLandState(id).plots[index]` уже ПОСЛЕ
вызова `landPlotPurchaseCost()`, прямо перед мутацией — тот же паттерн, что и в `zoneLandPlot`.
Ничего больше не менялось.

Проверено Playwright: до фикса `buyLandPlot('suburb')` не увеличивал число `owned` участков в
`districtLandState('suburb').plots` (баг воспроизведён); после фикса — `ownedBefore: 0` →
`ownedAfter: 1`, деньги списаны, 0 ошибок консоли. `saveVersion` не поднимался — формат данных не
менялся, только порядок чтения ссылки.

## 2026-09-21 — Сквозной аудит по чек-листу «anti-bug»: 2 бага найдены и исправлены

Отдельная задача — не жалоба на конкретную вещь, а прогон пользовательского чек-листа
(«проверять после каждого крупного апдейта»: сейвы, загрузка, ресет, престиж, старые сейвы,
спавн, коллизии, транспорт, очереди строек, отрицательные ресурсы, NaN/Infinity, утечки
памяти, мобильный FPS, resize) целиком против текущего состояния файла — впервые за сессию не
по кусочкам вместе с визуальной правкой, а отдельным полным проходом с Playwright-сценариями.

### 1. Найден и исправлен: кнопка «Сброс прогресса» не сбрасывала игру

`#resetBtn` показывал диалог, удалял `localStorage` (`tycoon3d_save_v3` и ещё 5 ключей) и звал
`location.reload()` — но `location.reload()` не останавливает JS синхронно: документ продолжает
исполняться до начала загрузки нового. На выходе браузер шлёт `pagehide`
(`addEventListener('pagehide', save)`) и часто короткую вспышку `visibilitychange`→
`document.hidden` (тоже `save()`), а в игровом цикле есть ещё периодический автосейв раз в 5
тиков экономики. Любой из них успевал выполниться между `removeItem(...)` и фактической сменой
документа и **заново записывал** только что удалённый сейв с ещё не сброшенным in-memory
состоянием — свежая страница читала его обратно. «Ресет» был no-op с рабочим на вид диалогом:
деньги/районы/купленные участки земли после «сброса» оставались идентичными состоянию до него
(деньги даже продолжали расти — экономика тикала в фоне до самой перезагрузки).

Фикс: флаг-предохранитель `suppressAutoSaveV36`, выставляется в `true` в обработчике
`#resetBtn` сразу после подтверждения диалога (до `removeItem`), и проверяется в самом `save()`
(`if (suppressAutoSaveV36) return false;`) — единая точка перекрывает разом `pagehide`,
`visibilitychange` и периодический автосейв, без необходимости оборачивать каждый из ~50 мест
вызова `save()` по файлу отдельно.

Проверено Playwright (5 повторных прогонов): до фикса — деньги/районы/владение участками после
«ресета» идентичны состоянию до него; после фикса — деньги на стартовых (300), районы обнулены,
участки земли не куплены, `localStorage.getItem('tycoon3d_save_v3') === null`,
`buildings.length === 0`, `stageIndex === 0`, 0 ошибок консоли.

### 2. Найден и исправлен: `upgradeLandPlot` молча теряла уплотнение участка (тот же класс бага, что `buyLandPlot`)

`upgradeLandPlot(id,index)` брал ссылку `p=districtLandState(id).plots[index]` **до** вызова
`landUpgradeCost(id,index)` — а та сама вызывает `districtLandState(id)`, которая безусловно
пересобирает `st.plots` в новые объекты на каждый вызов (санитизация старых сейвов). К моменту
`p.pendingLevel=level+1; p.pendingUntil=...` ссылка `p` уже отсоединена от реального массива —
деньги/ресурсы списывались (`tryPayResources` реально проведён), тост показывался, но само
уплотнение молча терялось. Ровно та же ловушка, что уже чинили в `buyLandPlot` в предыдущей
итерации — только `landUpgradeCost()`, а не `landPlotPurchaseCost()`, была источником лишнего
вызова `districtLandState()` между чтением и мутацией.

Фикс: тот же паттерн — `p` перечитывается заново из `districtLandState(id).plots[index]` после
`landUpgradeCost()`, непосредственно перед мутацией.

Проверено Playwright: до фикса — `moneyAfter < moneyBefore` и `woodAfter < woodBefore` (реально
списано), но `pendingLevel`/`pendingUntil` в `districtLandState(id).plots[0]` не менялись после
фикса; после фикса — те же списания, но `pendingLevel:2`, `pendingUntil:<будущее время>`.

### Остальные области чек-листа

- **Старые сейвы**: 6 представительных `saveVersion` (1, 5, 20, 21 — легаси плоский `housing`,
  30 — до участков земли, 41 — последняя версия до этой сессии) грузятся без исключений, числа
  конечные и неотрицательные. Чисто.
- **Престиж**: `doPrestige()` на полностью отстроенной компании + отстроенном городе — компания
  полностью сбрасывается (включая незавершённые площадки), город (районы/население/земля)
  сохраняется — подтверждённое намеренное поведение, не баг.
- **Спавн/коллизии**: свежий спавн `(0,0,0)`, `y=0`, в границах карты; 13 контрольных точек по
  полностью отстроенному городу (все районы ур.3, вся земля застроена) — ни одной встроенной в
  геометрию или проваливающейся точки, `resolvePlayerCircleCollisions` стабилен при повторном
  применении.
- **Транспорт/очереди строек**: `purchaseCurrentPad()` на несколько стадий подряд, время
  прогнано через `animate()` с движковым потолком `dt=0.075` — доставки грузовиком реально едут
  и завершаются. **Задокументированная некритичная находка** (не исправлялась): грузовик может
  простаивать неограниченно, если игрок абсолютно неподвижен `<1.15` юнита от его маршрута
  (маршрут от склада досок проходит рядом с хабом `(0,0,0)`, ровно точкой спавна) — встроенный
  watchdog перестраивает маршрут, но не может обойти *живое* препятствие. Самовосстанавливается
  моментально, как только игрок отходит (проверено). Система избегания трафика тонко настроена
  специально против deadlock'ов — правка ради маловероятного, самовосстанавливающегося крайнего
  случая не оправдана в этом проходе.
- **NaN/Infinity/отрицательные значения**: 3 стресс-сценария (свежая игра; всё максимально
  застроено, население 99 999 999; всё обнулено) по 3000-4000 кадров каждый на движковом
  потолке `dt`, сканирование `money`/`stats.*`/`cityState.*` рекурсивно — 0 находок.
- **Утечки памяти**: статически проверены все ~20 функций `refresh*()` — везде есть
  `disposeObject3D` (напрямую или через `clearGroupChildren`/`clearBaseWorld`/
  `clearSpecialProjectWorld`). Живой прогон — 60 раундов купить/зонировать/уплотнить/сбросить
  участки земли по всем 4 районам, `renderer.info.memory.geometries`/`.textures` идентичны
  (`1683`/`19`) от раунда 0 до раунда 59.
- **Resize/mobile**: `VISUAL_MOBILE` вычисляется один раз, ~45 использований консистентны; 5
  смен viewport подряд (телефон-портрет/ландшафт/планшет/десктоп/очень маленький) —
  `camera.aspect`/размер канвы всегда совпадают с `innerWidth`/`innerHeight`, 0 ошибок.

`saveVersion` не поднимался — оба фикса логические (порядок операций), не меняют форму
сохранения.

## 2026-09-21 — Детский сад: пятое и последнее гражданское здание Stage B

Отдельная задача (не жалоба на баг): из пяти гражданских зданий Stage B, изначально
намеченных в `TYCOON_V31_PLAN.md` («Больница/полиция/школа/детсад/парк»), в файле уже
были клиника (`CLINIC_BUILDING`), полиция (`POLICE_BUILDING`), школа (`SCHOOL_BUILDING`)
и парк (отдельная механика зелёной зоны) — детского сада не было. Реализован тем же,
многократно проверенным паттерном.

### Реализация

Найдено и переиспользовано (grep по актуальным, последним определениям — в этом файле
нет второго слоя переопределений для гражданских зданий Stage B, они определены один раз
и больше не трогались последующими version-блоками, в отличие от дорожной системы):
`CLINIC_BUILDING`/`POLICE_BUILDING`/`SCHOOL_BUILDING`, `civicBuildingPosition`/
`policeBuildingPosition`/`schoolBuildingPosition`, `CIVIC_DISTRICT_IDS`/
`POLICE_DISTRICT_IDS`/`SCHOOL_DISTRICT_IDS`, `occupiedBuildingPositions()`,
`districtHasClinic`/`districtHasPolice`/`districtHasSchool`, стекинг `serviceGrowth` в
`updateDistrictStats()`.

Добавлено (новый блок «v36 Stage B slice 5», сразу после блока школы):
- **`KINDERGARTEN_BUILDING`** (🧸 «Детский сад»/«Kindergarten»), архетип `shop`
  (переиспользован — 4 других архетипа уже заняты: `office`=жильё, `tower`=полиция,
  `house`=школа), цвет 0xff9fb0 (мягкий коралл-розовый — нейтрально-белый у клиники,
  тёмно-синий у полиции, жёлтый у школы, так что розовый визуально не путается ни с
  одним). Стоимость 850💰+3🪵+2🧱 (тот же диапазон, что у клиники/полиции/школы),
  `buildDuration` 5.0с. Доступен во всех 4 районах включая индустриальный
  (`KINDERGARTEN_DISTRICT_IDS = CITY_DISTRICTS.map(d=>d.id)`, тот же охват, что у
  клиники/полиции/школы), тот же конструкционный конвейер (`spawnConstructionSite`).
- **Визуальная уникальность сверх цвета**: `addKindergartenPlaygroundPropsV3610` —
  небольшая песочница (короб + насыпь песка) и качели (два столба, перекладина, сиденье,
  верёвки) добавлены прямо на меш архетипа `shop` — тот же приём, что уже используют
  декоративные добавки района (низкополигональные пропсы поверх базовой геометрии).
  Проверено: рендерится с 5 материальными цветами (`#e8b96a`/`#f3d9a0`/`#ff6f61`/
  `#ffd23f`/`#9c8a6b`), которых нет ни в одном из мешей клиники/полиции/школы.
- **Размещение**: `kindergartenBuildingPosition` — тот же ring-search
  (радиус 6+ring*2.4, 6 колец × 12 точек), тот же `distToNearestRoadSegment`/
  `distanceToDistrictArterial` клиренс-чек, что у клиники/полиции/школы, но пятое
  угловое смещение (`+Math.PI/3`, продолжая прогрессию клиники +π/12, полиции +π/6,
  школы +π/4), чтобы кандидатные точки на кольце не конкурировали с остальными четырьмя
  типами зданий при застройке одного района всем сразу. `occupiedBuildingPositions()`
  расширен пятым параметром `excludeKindergartenDistrict` (добавлен ПОСЛЕ существующего
  `excludeLandKey`, так что ни один из 4 старых вызовов с позиционными аргументами не
  задет — они просто не передают пятый аргумент).
- **Механика (обоснование выбора — почему не 6-й почти-дублирующий стат)**: детский сад
  тематически пересекается сразу с двумя уже существующими вещами — «образование»
  (раннее детство) и «привлекательность района для семей» (ёмкость жилья), а не с
  чем-то новым. Вместо изобретения 6-го `districtStats.*` поля:
  1. **Небольшой второй вклад в уже существующий `education`** (`updateDistrictStats()`,
     Pass 1): `educationTarget` теперь `29 + lvl*7 + (hasSchool?42:0) + (hasKindergarten?16:0)`
     — реальный, но явно МЕНЬШИЙ вклад, чем у школы (+16 против +42), чтобы не
     обесценивать смысл самой школы.
  2. **Отдельная, невзаимозаменяемая механика**: `KINDERGARTEN_CAPACITY_BONUS = 20` —
     плоский бонус к `districtHousingCapacity()` (при наличии построенного жилого дома
     в районе). Это поднимает сам ПОТОЛОК населения района, а не СКОРОСТЬ роста к нему
     — в отличие от аддитивного стекинга `serviceGrowth` (`1+clinic?0.26+police?0.18+
     school?0.22+fire?0.12+park?0.10`), которым уже пользуются все 4 других гражданских
     здания/парк. Кандидатский стат «детский сад делает район привлекательнее для
     семей» реализован буквально как бонус к жилой ёмкости — не ещё одна десятичная
     добавка в тот же список, а другой рычаг экономики. В индустриальном районе (без
     жилья) эта часть эффекта корректно равна 0 — бонус применяется только когда
     `apartmentBlockBuilt`, тот же guard, что и у остального расчёта ёмкости.
  3. Также включён (как и у всех остальных гражданских зданий) в
     `districtInfrastructureCount()` (влияет на satisfaction/jobs) и
     `cityServiceExpensesPerSec()` (+0.34💰/с эксплуатационных расходов — между парком
     0.30 и клиникой 0.58, соразмерно скромному масштабу здания).
- **Плейсхолдер-эффект удалён из UI-readout**: карточка района показывает статус
  «Детский сад построен · +20 к ёмкости жилья»/«Kindergarten built · +20 housing
  capacity» — конкретное число, а не абстрактный процент, чтобы игрок сразу видел,
  что именно делает здание (образование уже показывает школа на своей карточке, дублировать
  тот же процент на карточке детсада было бы избыточно).
- **Подключено во все параллельные системы** (grep-проверено, каждая — либо явно
  расширена, либо уже была generic/цикл по всем зданиям):
  - `cityWorldRuntime.kindergarten` (новая `Map`, тот же паттерн, что `civic`/`police`/
    `school`).
  - Коллизии игрока: `gatherPhysicsCircles()`/`agentObstacleCircles()` — новая строка
    цикла по `KINDERGARTEN_DISTRICT_IDS`, радиусы 1.55/1.48 (чуть меньше школьных
    1.60/1.52 — здание компактнее, `baseSize` 2.25 против 2.5).
  - Подъездные дороги/апроны: `drivewaySignatureNowV351` (сигнатура смены состояния),
    `refreshDrivewaysAndApronsV353` (апрон), `districtBuiltAccessTargets`
    (актуальная v35.2-редакция — генерирует ортогональный подъезд от районной улицы,
    `refreshDistrictAccessRoads` подхватывает автоматически), `cityBuildingRadiusAtV351`
    (радиус подъезда).
  - Пешеходная сеть резидентов: `footpathDistrictAnchorsV369` — детский сад добавлен
    как якорь замкнутого контура дорожек района, тем же способом, что школа/парк.
    `livingDistrictAnchors` (v35.2-редакция, общий список якорей района) — тоже
    расширен.
  - Смена языка: `applyLanguage()` теперь вызывает `refreshKindergartenWorld()`, чтобы
    надпись над зданием перевелась вместе с остальными при переключении RU/EN.
  - Панель «Город и развитие»: кнопка `data-kindergarten` на карточке района, ru/en
    подписи, статус-readout, обработчик клика зарегистрирован рядом с
    `data-school`/`data-park`.
  - `save()`: без изменений кода — `cityState` целиком сериализуется через
    `JSON.parse(JSON.stringify(cityState))`, новое поле `kindergarten` попадает в сейв
    автоматически, как и `civic`/`police`/`school` до него.
  - `load()`: новый блок миграции (тем же паттерном, что клиника/полиция/школа) —
    `cityState.kindergarten` отсутствует целиком на сейвах `saveVersion < 43`, каждый
    район по умолчанию получает `{kindergartenBuilt:false, pendingUntil:0, pos:null}`.
  - Инициализация: `refreshKindergartenWorld()` добавлен в цепочку `INIT` (после
    `refreshSchoolWorld()`), чтобы уже построенный детсад из загруженного сейва
    появлялся в сцене при старте страницы.
- **`saveVersion` поднят 42 → 43** с комментарием, обратно-совместимая миграция.

### Осознанно не тронуто

- **Сюжетная цепочка подсказок** (`currentGuideQuest`/`guideQuestV351`, главы 1-8) и
  **`GUIDE_MILESTONES_V351`** (разовые денежные награды за первое здание каждого типа) —
  не расширены детским садом. Прецедент — сам парк тоже НЕ входит в
  `GUIDE_MILESTONES_V351` (только housing/clinic/police/school/land/zoning/commerce/
  fire), хотя формально park участвует в `guideQuestV351` (глава 7). Учитывая
  многослойность и хрупкость этой системы (несколько последовательных переопределений
  `currentGuideQuest`, self-audit списка обязательных шагов на строках ~13216-13219 в
  актуальной нумерации) и то, что задача явно просит «тот же UX, что уже установлен для
  клиники/полиции/школы/парка» именно про панель/кнопку (а не про сюжетный туториал),
  решено не трогать — риск сломать хрупкую, самопроверяющую систему выше пользы для
  необязательного элемента.
- **Ежедневный маршрут резидентов** (`chooseResidentTarget`, живая финальная редакция
  v36.4 с раздельным расписанием по стилю района industrial/business/waterfront/default)
  — не добавлена отдельная «поездка в детский сад» как отдельный `activity`-шаг. Система
  уже переопределялась один раз (v36.4) и, по документированному прецеденту предыдущей
  итерации (пешеходные дорожки), повторная правка признана более рискованной, чем
  оправдано для этого захода — сам детский сад уже получает реальный игровой эффект
  через экономику (образование + ёмкость жилья) без необходимости менять поведение NPC.

### Проверено Playwright

- Чистая загрузка: 0 ошибок консоли (единственное сообщение за весь прогон —
  безобидное предупреждение браузера `navigator.vibrate` без пользовательского жеста,
  то же самое, что документировано в предыдущих итерациях, не связано с этой правкой).
- **Before/after числовая проверка эффекта** (район suburb, сначала построен жилой дом):
  до детсада `education=50`, `housingCapacity=60`; после постройки и полной сходимости
  экономики (80 тиков `updateDistrictStats()`) — `education≈66.0` (ровно +16, как задано
  формулой), `housingCapacity=80` (ровно +20 — `KINDERGARTEN_CAPACITY_BONUS`). Деньги/
  ресурсы реально списаны при постройке (199350→198500💰, 496→493🪵, 500→498🧱).
- **A/B-сравнение** (район industrial, без жилья): со школой, но без детсада
  `education≈92.0` (29+21+42, по формуле); с детсадом `education≈100.0` — упёрлось в
  верхний `clamp(0,100)` (92+16=108→100), что и ожидается и корректно демонстрирует
  работу клампа. `housingCapacity` в industrial остаётся `0` до и после (в районе нет
  жилья) — бонус ёмкости корректно не применяется там, где применять нечего.
- **Клиренс размещения** (численно, `distToNearestRoadSegment` + `occupiedBuildingPositions`):
  в suburb и industrial по отдельности — дистанция до дороги 15-26 юнитов (>> порога
  2.6), дистанция до ближайшего другого здания 6.0-7.3 юнита (>= порога `BUILDING_CLEAR`
  6.0 по построению алгоритма). Дополнительно — **полный прогон всех 7 типов одновременно
  в одном районе** (business: жильё, клиника, полиция, школа, парк, пожарная, детсад):
  все 7 успешно построены без конфликтов, детсад держит клиренс 6.59-16.33 юнита до
  каждого из 6 остальных зданий и 8.80 до дороги — минимальный зазор (школа, 6.59) всё
  равно выше порога.
- **Старый формат сейва** (`saveVersion: 42`, поле `cityState.kindergarten` отсутствует
  целиком): загружается без исключений, `cityState.kindergarten` во всех 4 районах
  корректно дефолтится в `{kindergartenBuilt:false, pendingUntil:0, pos:null}`,
  `districtHousingCapacity('suburb')` = 60 (без бонуса, как и должно быть), 0 ошибок
  консоли кроме уже упомянутого безобидного `navigator.vibrate`.
- **Панели**: «Город и развитие» рендерится с кнопкой `data-kindergarten` на карточке
  района; вложенные панели Автопарк/База/Операционный центр/Крупные тендеры (открыты
  через `#systemsBtn` → `#openFleetBtn`/`#openBaseBtn`/`#openOperationsBtn`/
  `#openTendersBtn`) открываются и закрываются без ошибок при построенном детсаде в игре.
- **Кнопка «Сброс прогресса»** (проверка фикса из предыдущей итерации в связке с новым
  зданием): построен детский сад (`kindergartenBuilt:true`, деньги ~199К) → нажат
  `#resetBtn` → подтверждён диалог → после перезагрузки: деньги вернулись к стартовым
  300💰, `cityState.kindergarten` во всех 4 районах снова `{kindergartenBuilt:false,
  pendingUntil:0, pos:null}`, `buildings.length===0`, `localStorage.getItem(...) ===
  null`, 0 ошибок консоли — ресет корректно затрагивает и новое здание.

## 2026-09-21 — Площадь квадратом + стадийные спики через сетку (риск-пасс из итерации 3) + найден и убран настоящий главный источник «каши»

Прямая эскалация пользователя после итерации 3 («щас какая-то помойка из дорог»): «Давай
перестроим город полностью, и площадь можно вообще убрать — у нас есть улицы квадратиками,
давайте наконец без каши из улиц». Итерация 3 сознательно ограничилась понижением яркости
ДАЛЬНИХ стадийных спиков (за пределами артериальной сетки), явно оставив «маршрутизацию
через сетку вместо прямой линии к хабу» как более рискованный заход на будущее — этот заход
теперь сделан, плюс убрана круглая площадь.

### 1. Площадь: круг → квадрат

`innerPlaza` (объявлена рядом со спавном, ~строка 1694 до правки) была
`THREE.CircleGeometry(4.35, 48)` с грунтовой текстурой. Заменена на
`THREE.PlaneGeometry(9, 9)` — тот же материал/текстура/цвет/высота, просто квадратная
форма вместо круглой, чтобы читаться как блок сетки, а не как чужеродный круг поверх
прямоугольной планировки. Больше нигде `innerPlaza` не используется (grep подтвердил) —
правка изолированная.

### 2. Стадийные спики: изгиб по осям сетки вместо прямой диагонали

Источник задачи — `STAGE_HUB_V367`/`stageRoadEndpointV367`/`makeStageRoadGroupV367`/
`refreshStageRoadsV367`/`spokeProjectV367`: 10 дорог от хаба (0,0,0) до каждого здания
основной линии прогресса (`STAGES`), позиции которых заданы спиралью (`stagePosition`:
угол = i·0.95 рад, радиус = 7 + i·4.6) — не на сетке.

Новая функция `stageRoadKneeV367(i)` вычисляет для каждого здания «колено» —
точку на той оси сетки (x=0 или z=0), к которой конечная точка здания ближе торцом
(`|end.x|>=|end.z|` → колено на x=0-оси на высоте `end.x`, иначе на z=0-оси на высоте
`end.z`). `stageRoadPolylineV367(i)` возвращает `[хаб, колено, конец]` — ломаная из
двух строго ортогональных отрезков (первый уже лежит на оси сетки, поэтому сам по себе
прямой; второй — короткий перпендикулярный подъезд к зданию). Тот же приём «посчитать
одно колено между двумя точками», что уже применяется в `addOrthogonalDrivewayV366` для
подъездных дорог района — никакого графа/pathfinding, чистая геометрия, считается один
раз за `refresh`, как и всё остальное в файле.

`makeStageRoadGroupV367` рисует оба отрезка (`addUnifiedRoadV365`) с той же логикой
ширины/стиля «внутри сетки / за её пределами», что была в итерации 3, плюс маленькая
тёмная асфальтовая заглушка на каждой уникальной точке колена
(`addStageRoadKneeCapsV3610`, тот же декоративный приём, что `addMasterGridJointCapsV368`
у самой сетки). Старый круглый «roundabout» на хабе (асфальтовое кольцо + зелёный
островок, оба `CircleGeometry`) убран — с квадратной площадью под ним он читался ровно
как «лишний кружок поверх решётки», чего пользователь и просил избежать.

**Маршрутизация грузовиков перепривязана к новой геометрии, не только отрисовка.**
`spokeProjectV367` (=`projectToRoad` в рантайме — последнее переопределение в файле, как
и раньше) теперь проецирует точку на всю ломаную (хаб→колено→здание), а не на прямой
отрезок хаб→здание, и возвращает новое поле `leg` (0 = отрезок хаб→колено, 1 =
колено→здание) плюс `knee`. Поле `segment` **не переименовано и не изменило смысл** —
это оказалось важно: `drivewayAnchorForPos` (используется `refreshDrivewaysV35` для
подъездных дорожек к каждому зданию) читает `segment` напрямую как индекс в
`ROAD_NODES` для вычисления касательной, и это совпадение семантики («индекс стадии»)
пришлось сохранить, иначе подъездные дорожки ко всем 10 зданиям стали бы смотреть в одну
и ту же сторону. `buildSpokeRouteV367` (=`buildRoadRoute`) вставляет точку колена в
маршрут между хабом и точкой на «подъездном» отрезке (`leg===1`) — грузовик едет
точка-на-подъезде → колено → хаб (и симметрично в обратную сторону), а не срезает
наискось через газон.

### 3. Главная находка: старая дорога-зигзаг с v34 всё ещё была активна

При визуальном аудите (широкие top-down скриншоты полностью отстроенного города —
все 4 района ур.3, все гражданские здания включая детсад, все участки земли, весь
10-здание стадийный маршрут) выяснилось: даже после пунктов 1-2 в местах, ГДЕ НЕ ДОЛЖНО
БЫТЬ никакой дороги (например у здания stage5, чей spur — меньше 1 юнита), на скриншоте
всё ещё виден толстый диагональный «домик»-силуэт (две диагонали, сходящиеся углом на
самом здании). Отладка через маркер-объект в точной мировой позиции здания подтвердила:
камера была правильно наведена — диагонали были настоящей геометрией сцены, не артефактом
рендера.

Источник — код у самого верха файла (рядом с объявлением `pathPoints`, ~строка 1710 до
правки, задолго до всей `v36.2`-`v36.9` дорожной переработки ниже по файлу): два `for`-
цикла рисовали ПОЛНОЦЕННУЮ дорогу (асфальт 2.3 юнита шириной + пунктирная осевая +
кромки + рем.швы + бордюр + тротуар с обеих сторон + пожарные гидранты через сегмент, и
отдельно круглые асфальтовые заглушки на стыках) вдоль цепочки `pathPoints` — а это
**хаб→stage0→stage1→...→stage9**, то есть зигзаг ПО ПОСЛЕДОВАТЕЛЬНЫМ зданиям спирали, а
не индивидуальные спики от хаба. Эти циклы добавляли меши напрямую в `scene` при загрузке
модуля (`scene.add(road)` и т.д., вне всякой `refresh*`-функции), поэтому их не касался
НИ ОДИН из последующих дорожных проходов сессии.

Более того — это была именно та вещь, которую `v36.7` («Stage Road Reset») по
собственному комментарию в `saveVersion` (`// v36.7 Stage Road Reset: removes legacy pale
spiral roads...`) должен был убрать: его чистка `removeLegacyStageRoadVisualsV367`
проверяет близость меша к прямой хаб→ОДНО-здание (`nearSpoke`, порог ~3.3 юнита) — это
верно ловит сегмент хаб→stage0 (совпадает с первым звеном цепочки) и круглые заглушки
ровно НА позициях зданий (радиус 1.1-1.45 — тоже совпадение по типу геометрии), но
сегменты МЕЖДУ соседними зданиями спирали (stage_i→stage_i+1) в общем случае далеко от
любой линии хаб→одно-здание и почти никогда не ловятся. Из 10+9 старых кусков дороги
чистка снимала едва ли пару — большинство переживало каждый прошлый «декluttер»-заход
непойманным. По ширине (2.3 юнита — шире почти всех остальных дорог в игре) и по стилю
(яркая жёлтая разметка) это, по всей видимости, и было главным источником ощущения
«каша из улиц» все эти итерации, а не только спики v36.7.

Правка: оба цикла удалены целиком (сама отрисовка — асфальт/разметка/кромки/швы/
бордюры/тротуары/гидранты/заглушки на стыках). Массив `pathPoints` и всё, что вычисляется
из него ниже по файлу (`ROAD_NODES`, `ROAD_CUM`, `distToNearestRoadSegment`, выборка
позиции гражданских машин на сплайне в `updateLivingCity`) **не тронуты** — они читают
`pathPoints` как чистые данные, не как источник для отрисовки, и не зависят от того,
рисуется дорога вдоль этой цепочки или нет. Материалы (`pathMaterial`/`lineMaterial`/
`roadEdgeMaterial`/`roadJointMaterial`/`curbMaterial`/`sidewalkMaterial`) оставлены
объявленными (используются только этим циклом — grep подтвердил, что просадки риска нет
от того, что они теперь не задействованы; убирать сами объявления не стал, чтобы не
трогать код без необходимости).

### Проверено (Playwright, `chromium.launch` с `--use-gl=swiftshader`)

- **Синтаксис**: `node --check` на извлечённом инлайн-скрипте — чисто.
- **Полная стройка через синтетический сейв** (все 10 STAGES ур.3, все 4 района ур.3,
  все гражданские здания включая детсад, все участки земли зонированы/ур.3) — 0 ошибок
  консоли на загрузке (кроме ожидаемых `navigator.vibrate` — блокируется хедлесс-браузером
  без жеста пользователя, не связано с правкой).
- **Визуально**: top-down ортографические скриншоты (обходят рендер-камеру игры, которая
  жёстко привязана к игроку с максимумом 20 юнитов — прямая установка `camera.position`
  немедленно перезаписывалась игровым `animate()`; для скриншотов использовалась отдельная
  `THREE.OrthographicCamera` с прямым `renderer.render(scene, cam)` при временно
  остановленном игровом цикле `requestAnimationFrame`) — широкий вид всей карты,
  приближенный вид хаба, и точечные виды на здания stage3/stage5/stage6/stage9: площадь
  читается как чистый квадратный блок, спики — как ортогональные Г-образные подъезды
  вдоль осей сетки, старый диагональный «домик»-силуэт у зданий исчез. Остаются
  небольшие диагональные соединители «угол сетки → район» (`districtConnectorPathV362`,
  не трогался — вне запроса пользователя, читается как разумный съезд, а не как
  солнечные лучи) и короткие диагональные подъезды отдельных участков земли к ближайшей
  внутренней улице района (тоже не трогались).
- **Доставка грузовиком по новому маршруту — реально проверена, не просто вызов функции**:
  синтетический сейв со зданием stage4 в процессе стройки (`waitingForDelivery:'planks'`,
  таймер у самого гейта поставки). Обход троттлинга `requestAnimationFrame` в хедлесс-
  вкладке (реальные секунды ожидания давали ~1 кадр/сек — слишком медленно) — прогнан тот
  же покадровый апдейт, что и `animate()`, с фиксированным `dt=0.075` в плотном цикле
  (детерминированное симулированное время вместо ненадёжного wall-clock). Таймлайн
  состояния грузовика: `to_source` (едет на лесопилку) → `outbound` (едет на объект,
  позиция реально смещается вдоль оси x≈0/z≈0 — т.е. по новому «стволу» маршрута) →
  на отметке ~13.5 симулированных секунд `delivered:[true]` и позиция грузовика
  (-20.33,-15.31) — вплотную к реальной позиции здания stage4 (-22.23,-17.19) — →
  `returning` → `idle` дома. 0 ошибок консоли на всём протяжении.
- **`distToNearestRoadSegment` клиренс-сэмпл** (625 точек по сетке 25×25 по всей карте,
  та же методика, что в итерации 3) — `min:0, max:59.74, avg:18.77` — конечные разумные
  числа, `pathPoints` не тронут, поведение ожидаемо идентично до/после (массив-источник
  не менялся).
- **Гражданский трафик** — отдельная система (`livingCityRuntime.cars`, использует
  `ROAD_NODES`/`ROAD_CUM`, не связанные с `spokeProjectV367`) — прогнан тот же
  покадровый апдейт с фиксированным `dt`, позиция машины меняется кадр к кадру
  (не застряла), подтверждено что система не задета правкой.
- **Старый сейв** (`saveVersion: 1`, плоский `housing`, 3 здания) — грузится и
  мигрирует без ошибок, `stageRoads` перестраивается (106 мешей — то же число, что и на
  свежей/полной игре, поскольку все 10 спиков рисуются независимо от того, сколько
  зданий реально построено).
- **Свежая игра без сейва** — 0 ошибок, `innerPlaza.geometry.type === 'PlaneGeometry'`
  подтверждено программно.
- **Панели** — «Город», «Системы», «Автопарк», «База», «Контракты», «Операции»,
  «Проекты», «Бонусы» — все рендерятся без исключений на полностью отстроенном городе.

`saveVersion` не поднимался — все правки чисто визуальные/геометрические (форма площади,
форма дорожной геометрии, маршрутизация), сериализованный формат сейва не тронут.

### Осознанно оставлено на будущее

Диагональные соединители «угол артериальной сетки → район» (`districtConnectorPathV362`)
и короткие диагональные подъезды участков земли к внутренней улице района — не входили в
запрос этой итерации (пользователь просил про площадь и про стадийные спики конкретно),
заметно меньше по масштабу/яркости, чем то, что было убрано, и трогать их — отдельный,
самостоятельный риск (`masterRoadSegmentsV362`/районные подъезды явно указаны как
read-only в задаче на этот заход). Если пользователь позже укажет и на них — это
следующий кандидат в том же духе, тем же приёмом (`stageRoadKneeV367`-подобное колено).

## 2026-09-21 — «Постройте вообще всё через эти кнопки, даже дороги и фонари»

Прямая просьба пользователя (перевод с русского): «Я бы хотел, чтобы буквально всё
строилось через эти кнопки [панель "Город и развитие"], чтобы даже дороги и фонари
строились так же». Полный разбор объёма и обоснование, что осталось за скобками — в
`TYCOON_V36_PLAN.md`, итерация 9. Коротко здесь — что именно сделано и как проверено.

### Реализация

1. **`cityState.streetlights[id]`** (новый top-level блок, 4 района, тот же паттерн
   `{флаг, pendingUntil}`, что у парка/пожарной части) + кнопка «Осветить улицы»/
   «Build streetlights» на карточке района (видна при `districtLevel(id)>=1`), стоимость
   380+90×ранг💰+1🪵, установка ~2.6с. По завершении — `refreshStreetlightsWorld()` рисует
   6 столбов `makeLamp()` по тротуарному кольцу района (`districtRingRoadPoint(...,4.30)`)
   с усиленным `emissiveIntensity` (1.35) плюс дешёвый аддитивный «блик»-декаль под каждым
   (без новых `THREE.PointLight` — в файле их нет вообще, мобильная производительность).
   Эффект: `+10` к `safetyTarget` района в `updateDistrictStats()`.
2. **`cityState.districtRoads[id]`** (тот же паттерн) + кнопка «Проложить дороги района»/
   «Build district roads» (видна при `districtLevel(id)>=1`), стоимость 520+140×ранг💰+
   1-2🪵+(0-1🧱 на ранге 3), «прокладка» ~3.4с. По оплате включается
   `cityState.districtRoads[id].roadsBuilt`, и только тогда подъездные дороги-спики от
   ПОСТРОЕННЫХ гражданских/жилых/коммерческих зданий района к его уличному кольцу
   (`districtBuiltAccessTargets`→`refreshDistrictAccessRoads`) и их мощёные «фартуки»
   (`cityAccessApronV353` в `refreshDrivewaysAndApronsV353`) реально появляются — раньше
   это происходило бесплатно и автоматически, как только `districtLevel(id)>0` и само
   здание было достроено. Эффект: `+8` к `roadRelief` в формуле `trafficTarget`
   (реальное снижение трафика района).
3. **Гейтинг без правки исторического дорожного кода.** Вместо редактирования любого из
   множества переопределений `refreshDistrictAccessRoads`/`districtBuiltAccessTargets`,
   разбросанных по файлу (v34.3→v35.2→v35.3→v36.5→v36.6, каждое — обёртка над предыдущим),
   в самом конце файла добавлена ЕЩЁ ОДНА обёртка (тот же идиоматичный приём) поверх
   финальной активной версии `districtBuiltAccessTargets` и `cityAccessApronV353`: обе
   становятся no-op, пока `!districtRoadsBuilt(id)`. Затрагивает ТОЛЬКО спики/«фартуки»
   отдельных зданий — уличное кольцо района (`makeCityStreetGroup`, по `districtLevel`),
   артериальная связь (`refreshArterialRoads`), автобусная остановка
   (`districtLevel>=2`, отдельная `districtStopWorldPointV364`-система) и подъезды
   земельных участков (`addPullInBayV353`, независимый цикл) — не тронуты вообще.
   Подтверждено (grep + чтение кода): ни пешеходный клиренс резидентов
   (`distToNearestRoadNetworkV369`), ни маршруты доставки (`serviceAccessGraph`) не читают
   эти меши — чисто косметический слой.
4. **Миграция.** `roadsBuilt` по умолчанию `false`, но сейв БЕЗ ключа `districtRoads`
   вообще (то есть сохранённый до этой итерации) при загрузке дедушкуется: для района,
   чей `districtLevel` в сейве уже `>=1`, `roadsBuilt` сразу `true` — старые, уже
   отрисованные под авто-системой дороги не пропадают молча. Сейв, у которого ключ уже
   есть (`saveVersion>=44`), доверяется как есть, повторного дедушкования не происходит.
   `streetlights` — новая фича без предыстории, мигрировать нечего.

`saveVersion` 43→44. UI на обеих кнопках — ru/en, тот же формат, что у всех остальных
построек.

### Проверено (Playwright, `chromium.launch` с `--use-gl=swiftshader`)

- `node --check` на извлечённом инлайн-скрипте — чисто.
- Свежая загрузка без сейва — 0 ошибок консоли.
- **Полный цикл через настоящие кнопки UI**: район прокачан до уровня 1, построена
  клиника, скриншот ДО оплаты — клиника видна, подъезда к кольцу и фонарей нет; клик по
  обеим новым кнопкам в реальной DOM-панели; скриншот ПОСЛЕ — 6 фонарей на кольце района
  и подъездная дорога к клинике появились ровно в этот момент, не раньше (`streetlightMesh:
  12` детей меша, `accessRoadsMesh: 0→1`). `safety`-стат района вырос. 0 ошибок консоли.
- **Старый сейв** (эмулирован `saveVersion:43`, без `streetlights`/`districtRoads`, район
  уровня 2 с уже построенной клиникой) — после загрузки `districtRoadsBuilt===true`
  (дедушкование сработало), подъезд+«фартук» клиники сразу отрисованы (4+2 меша), деньги/
  уровень района из старого сейва подтверждены загруженными, 0 ошибок консоли.
- **«Современный» сейв** с явным `roadsBuilt:false` — подтверждено, что повторного
  дедушкования НЕ происходит.
- **Гражданский трафик/резиденты** — после полной стройки через кнопки в двух районах,
  `livingCityRuntime` показывает живые машины (20) и резидентов (24), не задеты.
- **`window.__TYCOON_ROAD_AUDIT__.snapshot()`** — `issues: []`, `pairs: []` после стройки.
- **Кнопка сброса** — после стройки через новые кнопки, клик «Сброс» → новая загрузка →
  `streetlights`/`districtRoads`/`districtLevel` корректно вернулись к дефолту,
  localStorage очищен.
- **Панели** «Город», «Автопарк», «База», «Тендеры» — рендерятся без исключений на
  полностью отстроенном (через новые кнопки) городе.

### Осознанно оставлено вне объёма

Полноценный тайловый редактор дорог (свободная прокладка игроком дорог произвольной формы
в любой точке карты) — прямо исключён при сужении объёма задачи: отдельный, самостоятельный
инструмент с нуля, не расширение существующего паттерна кнопок, и риск совершенно другого
масштаба для уже 8 раз правленной за сессию дорожной системы. Также не тронуты: автобусная
остановка на въезде в район (тот же рычаг «уровень района», не про дороги конкретно);
подъезды земельных участков (уже полностью платная система); само уличное кольцо района и
артериальная связь с главной дорогой (уже де-факто платные — идут вслед за кнопкой «Развить
район», трогать глубоко вложенные в них гражданский трафик/пешеходные маршруты ради
переименования уже-платного действия — риск без пользы игроку).

## 2026-09-21 — Закрыт последний диагональный хвост: коннекторы «сетка → район» и подъезды участков

Ранее в этой же сессии итерация «площадь + спики» (`790c2b0`/`7e1eb4d`) явно отложила два
МЕНЬШИХ диагональных участка дорожной сети как «не входило в запрос этого захода» (см.
раздел «Осознанно оставлено на будущее» в записи от того же дня): диагональные соединители
`districtConnectorPathV362` («угол артериальной сетки → район») и короткие диагональные
подъезды земельных участков к внутренней улице района. Пользователь неоднократно и прямо
жаловался на «помойку из дорог» несколько заходов подряд, поэтому этот хвост закрывается
сейчас тем же приёмом, что уже доказал себя на спиках стадий (`stageRoadKneeV367`) и на
подъездах гражданских зданий (`addOrthogonalDrivewayV366`): один прямой угол вместо
диагонали.

### 1. `districtConnectorPathV362` — коннектор «угол сетки → район»

**Было.** Три сегмента: `start → mid1 → mid2 → hub`, где `mid1=((start.x+hub.x)/2, start.z)`
и `mid2=(hub.x, (start.z+hub.z)/2)`. Первый и третий сегменты были осевыми, но средний
(`mid1 → mid2`) одновременно менял и x, и z — то есть был настоящей диагональю,
замаскированной под «колено» (эта деталь и была той самой оставленной «небольшой диагональю»
из предыдущей записи).

**Стало.** Настоящее колено на 90°, тот же принцип, что и `stageRoadKneeV367`: считается
`dx=start.x-hub.x`, `dz=start.z-hub.z`, и колено ставится на доминирующей оси —
`Math.abs(dx)>=Math.abs(dz) ? (start.x, hub.z) : (hub.x, start.z)`. Путь — `[start, knee,
hub]` (вырожденные точки короче `.7` отфильтровываются, как и в старом коде). `start`/`hub`
(входные и выходные точки) не менялись, поэтому всё, что читает только их
(`districtArterialEndpoints`, автобусная остановка на въезде в район), не задето.

**Кто использует путь целиком (не только концы) — и почему это безопасно.** Проверено
grep'ом и чтением кода: `distanceToDistrictArterial` (клиренс размещения зданий/декора —
десятки мест: `distToNearestRoadSegment(...)||distanceToDistrictArterial(...)`),
`distToNearestRoadNetworkV369` (клиренс пешеходных маршрутов резидентов), и рисовка
(`refreshArterialRoads`, обе версии — v36.5 и финальная v36.7-эры — и
`refreshDistrictAccessRoads` в её раннем варианте) — все проходят по `path[i]/path[i+1]`
универсальным циклом по сегментам, без предположений о числе точек или их геометрии. Смена
формы пути (2 сегмента вместо 3, с другими координатами середины) не требует правок ни в
одном из этих потребителей — ровно то же наблюдение, что уже подтвердилось на спиках стадий.

### 2. Диагональные подъезды земельных участков

**Было.** В `refreshDrivewaysAndApronsV353` (текущая живая реализация `refreshDrivewaysV35`
— проверено grep'ом всех переопределений: `12678`→`12932`→`13114`→`13318`, последнее
побеждает) для каждого зонированного участка вызывался `addPullInBayV353(...,
Math.atan2(dx,dz), ...)`, где `dx/dz` — вектор от `districtRingRoadPoint(id,
districtAngleToPoint(id,pos), 3.78)` (точка на прямоугольном периметре района ПОД СЫРЫМ
УГЛОМ от центра к участку) до `districtBuildingFront(id,pos)`. Поскольку
`districtRingRoadPoint` — это точка на прямоугольной границе на произвольном непрерывном
угле (не привязана к сетке), а участки земли стоят вокруг района не строго по осям, этот
вектор почти всегда диагональный — короткая, но заметная диагональная «заплатка»/площадка
у каждого купленного и застроенного участка.

**Стало.** Тот же приём, что уже применяется к гражданским зданиям (`addOrthogonalDrivewayV366`
+ `nearestDistrictStreetTargetV366`), теперь применяется и к участкам: вместо абстрактной
точки на периметре по сырому углу цель — РЕАЛЬНАЯ ближайшая точка на настоящей уличной сети
района (крест + периметр, та же геометрия, что рисует `makeCityStreetGroup`). Декоративная
площадка (`addPullInBayV353`) осталась, но её `yaw` больше не сырой `atan2` — он берётся из
`nearestDistrictStreetTargetV366`'s `kind` (`'v'`/`'h'`) и снаппится на ближайший
кардинальный угол (`0`/`π`/`±π/2`) в направлении первого (примыкающего к участку) отрезка
проезда.

**Побочный эффект (в плюс).** `addOrthogonalDrivewayV366` целится в настоящую уличную сеть
района, а не в абстрактную точку на периметре — то есть подъезд участка теперь физически
доходит до реальной мощёной улицы вместо того, чтобы заканчиваться в открытом поле рядом с
ней, как раньше.

### Проверено (Playwright, `chromium.launch` с `--use-gl=swiftshader`)

- **Синтаксис** — извлечённый инлайн-скрипт (877548 символов) проходит `node --check` без
  ошибок.
- **Полная стройка через настоящие игровые функции** (не прямая запись флагов): деньги/
  ресурсы выданы с запасом, `cityState.districts[id]=3` для всех 4 районов напрямую (это
  единственное прямое присваивание состояния во всём тесте — сразу удовлетворяет и
  `districtLevel>0`, и `districtUnlocked`), дальше — только реальные обработчики кнопок
  (`buildResidentialBlock`, `buildCivicBuilding`, `buildPoliceStation`, `buildSchool`,
  `buildKindergarten`, `buildFireStation`, `buildDistrictPark`, `buildCommercial`,
  `buildStreetlights`, `buildDistrictRoads`, `buyLandPlot`, `zoneLandPlot`,
  `upgradeLandPlot`), с прогоном реального тика завершения стройки (`updateCityExpansion`,
  `updateLandDevelopmentCompletions`) после перевода `pendingUntil` в прошлое. Итог:
  все 4 района ур. 3/3, все гражданские здания включая детсад и пожарную часть построены во
  всех 4 районах, все 12 участков земли (3×4) куплены/зонированы/застроены до уровня 3,
  фонари и дороги района куплены через новые кнопки во всех 4 районах — **0 ошибок консоли**
  на протяжении всей стройки.
- **Визуально, широкий вид сверху** (отдельная `THREE.OrthographicCamera`, `scene.fog`
  временно обнулён на время рендера скриншота — иначе `Fog(0xcfe9ff, 40, 175)` с камеры на
  высоте 200 просто заливает кадр туманом, дистанция-то от камеры, не от земли; сразу
  восстановлен после рендера): вся сеть дорог читается как чистая прямоугольная сетка —
  главный крест+периметр, квадратная площадь-хаб, ортогональные спики стадий (не тронуты,
  подтверждён их прежний вид), и все 4 коннектора «угол сетки → район» идут строго вдоль
  осей с одним прямым углом, без единой диагонали. Приближенные виды на каждый из 4 районов
  (коннектор крупным планом + участки земли крупным планом, 8 скриншотов) подтверждают то же
  на уровне отдельных подъездов зданий/участков — единственные диагональные/изогнутые линии
  на кадрах — это пешеходные тропинки резидентов (`refreshResidentFootpathsV369`, другой
  цвет/материал, намеренно органичные, вне периметра этой задачи и не тронуты).
- **`distToNearestRoadSegment` клиренс-сэмпл** (676 точек, сетка по всей карте) —
  `min:0.015, max:59.74, avg:18.72` — числа в одном порядке с предыдущими итерациями
  (`avg:18.77` в записи «дороги-помойка»), `pathPoints` не тронут.
- **`window.__TYCOON_ROAD_AUDIT__.snapshot()`** на полностью отстроенном городе —
  `issues: [], pairs: []`.
- **Гражданский трафик** — 12 видимых машин, все 12 сдвинулись за 60 кадров симуляции с
  фиксированным `dt=0.075`; 24 резидента в `livingCityRuntime.residents`.
- **Маршрут доставки** — `buildRoadRoute(hub, clinicPos)` вернул конечный (4 точки, все
  finite) маршрут после смены геометрии коннектора — подтверждает, что
  `districtArterialPath`/`spokeProjectV367`-based routing не задет (доставка не читает
  `districtConnectorPathV362` напрямую, только через `distanceToDistrictArterial`
  клиренс-проверки, которые универсальны к форме пути).
- **Панели** — `renderCityPanel`, `renderSystemsPanel`, `renderFleetPanel`, `renderBasePanel`,
  `renderTendersPanel`, `renderOperationsPanel` вызваны напрямую на полностью отстроенном
  городе — все `ok`, без исключений.
- **Кнопка сброса** — отдельный прогон: район прокачан до ур.3, фонари и дороги района
  куплены и достроены, `save()`, затем реальный клик по `#resetBtn` (с авто-принятием
  `confirm()`) → `location.reload()`. После перезагрузки: `districts.suburb===0`,
  `streetlights.suburb.streetlightsBuilt===false`, `districtRoads.suburb.roadsBuilt===false`,
  `localStorage` без ключа сейва, деньги — на стартовом значении (300). 0 ошибок консоли.

`saveVersion` не поднимался — обе правки чисто геометрические (форма пути/ориентация
декоративной площадки), сериализованный формат сейва не тронут (проверено grep'ом: ни одно
поле не добавлено/переименовано).

### Осознанно оставлено на будущее

С закрытием этих двух пунктов ни один из явно задокументированных в этой сессии диагональных
участков дорожной сети не остаётся — единственные оставшиеся не-осевые линии на карте это
пешеходные тропинки резидентов, которые по конструкции (`refreshResidentFootpathsV369`,
итерация 4) намеренно органические/изогнутые как отдельная от машин система, а не баг
дорожной сети, и явно вне запроса пользователя («жители не должны ходить по дорогам как
машины» — цитата итерации 4). Полноценный тайловый редактор дорог по-прежнему не входит в
объём (см. запись «постройте вообще всё через эти кнопки» — обоснование не изменилось).

## 2026-09-22 — Исправлен зависающий грузовик (баг, отложенный в аудите)

Ранее задокументированный, но не исправленный баг: грузовик доставки может встать
намертво, если игрок стоит неподвижно в пределах ~1.15 юнита от его маршрута у
центрального хаба — система избегания препятствий обнуляет скорость грузовика рядом с
игроком, а сторож восстановления (recovery watchdog) просто пересчитывает тот же
маршрут, который снова упирается в ту же точку.

Фикс (`moveWorkVehicle`): когда сторож восстановления срабатывает (простой > 2.6с) и
именно игрок оказывается препятствием рядом (< 2.6 юнита), грузовик сначала считает
короткий боковой манёвр в сторону от текущей позиции игрока (перпендикулярный сдвиг на
2.2 юнита, в сторону, где уже чище), и едет через эту точку к цели вместо простого
повторного маршрута в ту же стену. На время манёвра (1.6с) радиусы
остановки/торможения возле игрока временно сжимаются (1.15→0.45, 2.35→1.15), чтобы
грузовик реально мог выполнить манёвр, а не замереть заново в ту же секунду. Обычные
радиусы возвращаются сразу после окончания окна манёвра.

Эта правка была начата фоновым агентом, который упал по лимиту сессии на середине
работы (оставив незакоммиченную, но синтаксически корректную и логически цельную
правку в рабочей копии) — я перепроверил её сам перед коммитом.

**Проверено:**
- `node --check` на извлечённом инлайн-скрипте — синтаксис чист.
- Чистая загрузка — 0 ошибок консоли.
- Реалистичный сценарий (игрок стоит в 0.9 юнита от грузовика, не двигается): простой
  копится до ~2.57с (совпадает с порогом 2.6), затем срабатывает манёвр, грузовик
  реально начинает двигаться и проезжает >1.2 юнита за **~3.53с суммарно** — раньше в
  этом сценарии он завис бы, пока игрок не сдвинется (могло быть сколь угодно долго).
- **Найден и честно задокументирован остаточный краевой случай**: если игрок стоит
  ТОЧНО в той же точке, что и грузовик (дистанция ровно 0, не просто «рядом»), сжатый
  радиус остановки (0.45) всё равно больше 0, так что манёвр вычисляется и назначается,
  но грузовик всё ещё не может тронуться с места в течение всего окна манёвра — проверено
  трассировкой по кадрам. Это не постоянный дедлок: по истечении окна манёвра сторож
  срабатывает заново (симптом циклически повторяется каждые ~4.2с), но и не решается
  окончательно в этом конкретном пограничном случае. Практически маловероятный сценарий
  (координаты игрока и грузовика совпадают день-в-день), но честно отмечен как известное
  ограничение, а не выдаётся за полное решение.
- `saveVersion` не поднимался — правка живого состояния движения, не сохраняется.
