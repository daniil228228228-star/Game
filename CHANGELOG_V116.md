# Changelog — Tycoon v116

## 2026-09-26 (шестой заход) — Индустриальный кластер: силос/миксер/лесопилка выглядели «плоско»

Пользователь прислал скриншот индустриального кластера (лесопилка/бетонный завод/конвейер) после
фикса раскосов конвейера (1d57116) с отзывом: «Всё прекрасно, но переделай пожалуйста этот завод и
конвеер, сделай красивее и лучше» — чисто художественный запрос, геометрия к этому моменту уже
корректна (см. changelog ниже), но читается утилитарно: плоский серый силос-цилиндр, безликий
валик миксера, коробки без отделки.

**Важное уточнение объёма работ, полученное в середине захода.** Пока эта работа шла, пользователь
отдельно попросил убрать сам приподнятый мост конвейера («убери этот конвеер который на высоте
находится») — отдельный, более крупный заход намерен перенести всю индустриальную зону (лесопилка,
бетонный завод, металлический склад, автопарк, склады) в отдельную зону вдали от жилой застройки, и
после переноса нужда в мосте через ту самую дорогу скорее всего отпадёт (мост со всеми опорами и
раскосами из 1d57116, вероятно, будет заменён обычным наземным конвейером на новом месте). Поэтому
этот заход **сознательно не трогает геометрию моста/раскосов/рамп конвейера** (ленты, `beltMaterial*`,
`addConveyorSpanBox`, `addConveyorSupport` — всё как было после 1d57116) — риск, что работа над этой
геометрией будет выброшена ближайшим же следующим заходом, признан не стоящим вложенных усилий.
Весь художественный полишинг в этом заходе ограничен зданиями/оборудованием, которые переживут
перенос независимо от итоговой высоты и положения конвейера: `buildConcretePlant()` (силос,
миксер) и здания в `buildSawmillScenery()` (офис/склад лесника, здание лесопилки) — сам конвейер
(ленты/опоры/раскосы моста, `tycoon-v116.html:3051-3159` в текущей нумерации, внутри той же
функции) не менялся ни на строку.

### 1. Бетонный завод (`buildConcretePlant()`, `tycoon-v116.html:3178-3378` после правки)

Было: один плоский `CylinderGeometry` силоса + `ConeGeometry` крышка одним и тем же серым
металлическим материалом, 4 голых стойки-подпорки без связи между собой, и барабан миксера,
целиком залитый одним бетонным оттенком (`createConcreteMaterial(0xc9c3b6)`) — визуально читался
как «труба» и «валик», как и описал пользователь.

Изменения (тот же единственный владелец функции, без нового патч-слоя):
- Силос: тело переведено на `createCorrugatedMaterial` (гофрированная сталь, переиспользует
  встроенную текстуру `assets_v31/metal.jpg` через существующий `getCorrugatedTexture`) вместо
  плоского металла; добавлены 2 воротника-коллара другого оттенка на стыке тело/крышка, крышка
  перекрашена в контрастный ржаво-рыжий металл (`0xa8532f`), сверху — маленький красный
  проблесковый маячок (эмиссивная сфера).
- Кольцо предупреждающих жёлтых полос у основания силоса (12 боксов по окружности) — переиспользует
  существующий по всему файлу приём «однотонные жёлтые повёрнутые боксы» (см. ветку
  `type === 'warehouse' || type === 'factory'` в архетипных деталях, `tycoon-v116.html:6128`),
  вынесенный в переиспользуемую `addHazardChevrons()` (`tycoon-v116.html:4807-4817`) для прямых
  участков и отдельным инлайн-циклом для кольца (кольцу нужен свой угол на каждый штрих,
  `addHazardChevrons` рассчитана на прямую).
- Служебная лестница (2 рейки + 8 перекладин) с двумя предохранительными полукольцами
  (`TorusGeometry`, `Math.PI` arc) и небольшая смотровая площадка с перилами у верха — тот же
  приём рейка+перекладина, что уже использовался для другой лестницы в файле.
- 4 опоры силоса теперь связаны горизонтальной рамой (4 бокса) у верха стоек вместо 4 независимых
  «голых кольев».
- Миксер: барабан перекрашен в металлик цвета строительной техники (`0xd9a520`) вместо единого
  бетонного оттенка, добавлены два кольца-обода (`TorusGeometry`) на концах барабана как дети
  самого `drum`-меша (наследуют его `rotation.z`, поэтому корректно охватывают его без ручного
  пересчёта базиса); у приводного (широкого) конца добавлен видимый редуктор-коробка + шкив —
  раньше барабан крутился без единой видимой причины; добавлены 4 опорные ножки под основанием,
  жёлто-чёрная предупреждающая полоса на переднем крае основания (`addHazardChevrons`) и щиток
  управления с двумя индикаторными лампочками (зелёная/красная, эмиссивные сферы).
- Труба подачи (`addPipeSpan()`, новый переиспользуемый хелпер рядом с другими `add*`-хелперами,
  `tycoon-v116.html:4790-4802` — по той же схеме «вычислить длину + сориентировать через
  quaternion», что уже применяется для секций ленты конвейера, `addConveyorSpanBox`, но без
  профиля высоты, который там нужен только самой ленте) визуально соединяет выход силоса со входом
  миксера — раньше два агрегата стояли рядом без всякой связи.
- Небольшая операторская будка (кремовый сайдинг `createSidingMaterial(0xefe6d6)`, красная крыша
  `createRoofMaterial(0xb3352a)` через `createGableRoof`, одно окно с синей вставкой через уже
  существующий `addFramedWindow()`, коричневая деревянная окантовка двери) — третье
  материальное/кровельное семейство на участке завода вместо «два станка + вывеска»; та же палитра
  (кремовый сайдинг + красная крыша + синее окно + коричневая окантовка), которую пользователь сам
  указал как образец качества у соседних домов.

### 2. Здания лесопилки (`buildSawmillScenery()`, только офис и цех — лента и опоры не тронуты)

- Офис/склад лесника (`camp`): угловые накладки-пилястры (`addCornerPillars`, тот же приём, что
  использует `buildHouse()` для домов), накладка-конёк на крыше и вентиляционная труба на скате
  (`addRoofVent`, уже существующий хелпер) — из плоской обшитой коробки с одним оттенком крыши.
- Цех лесопилки (`mill`): угловые накладки (`addCornerPillars`), вентиляционная труба на крыше,
  служебная лестница вдоль заднего угла (рейки+перекладины) и труба вдоль стены — цех был плоской
  коробкой с окнами и одной вентшахтой; и предупреждающая жёлтая полоса на кожухе пилы
  (`addHazardChevrons`) прямо у вращающегося диска, у которого раньше не было вообще никакой
  маркировки опасности.

### Полигоны

Точный подсчёт (не по всей сцене — она содержит рандомизированный скаттер/архетипы построек,
чей общий счётчик расходится от перезапуска к перезапуску независимо от этой правки; вместо этого
3 конкретные группы найдены по их мировой позиции — `GENERATOR_POS`/`SAWMILL_POS`/
`CONCRETE_PLANT_POS` — и обойдены по треугольникам напрямую,
`scratchpad/factoryart116/tricount.mjs`):

| узел | до | после | Δ |
|---|---|---|---|
| офис лесника (`camp`) | 268 tris / 23 mesh | 352 / 30 | +84 / +7 |
| цех лесопилки (`mill`) | 464 / 32 | 744 / 53 | +280 / +21 |
| бетонный завод (`plant`) | 328 / 13 | 2044 / 74 | +1716 / +61 |
| конвейер (не менялся, контроль) | 836 / 33 | 836 / 33 | 0 / 0 |

Итого +2080 треугольников на 3 узла (~+1.1% от типичного общесценового счётчика ~180k tris) —
сосредоточено ровно в тех двух объектах, на которые жаловался пользователь; конвейер как контроль
подтверждает, что изменения не затронули ничего за пределами заявленного объёма. Мелкие повторяющиеся
детали (полосы, перекладины, ножки) сделаны отдельными мелкими мешами по уже устоявшемуся в файле
приёму для однократно строящихся зданий (см. `buildFactory`/`buildHouse` — рейки, трубы, ступени
везде отдельными мешами, не `InstancedMesh`); `InstancedMesh` в файле зарезервирован для
по-настоящему массового скаттера (сотни экземпляров, `createGroundMicroDetail()`), что не
относится к деталям одного здания, строящегося один раз за сессию.

### Скриншоты

`scratchpad/factoryart116/before_wide.png` / `after_wide.png` — общий план кластера (силос+миксер
+будка+офис) с ракурса, близкого к скриншоту пользователя (на `after` виден новый вентиляционный
короб на коньке крыши офиса, силос с гофрой, красная крыша будки за миксером). `before_concrete_close.png` /
`after_concrete_close.png` — силос/миксер крупным планом (гофра, лестница, воротники, кольца
барабана). `before_mill_exterior.png` / `after_mill_exterior.png` — цех лесопилки снаружи (на
`after` виден новый вентиляционный короб на коньке крыши цеха, отсутствующий на `before`). Тестовые
скрипты и raw-скриншоты не закоммичены (`scratchpad/factoryart116/`, по методологии проекта).

### Регрессия

- `node --check` по всем 63 непустым `<script>`-блокам (`scratchpad/check_blocks.mjs`) — без
  ошибок, блоков по-прежнему 63 (новых патч-слоёв не добавлено; правка — в исходных функциях
  `buildConcretePlant()`/`buildSawmillScenery()`).
- 10 полных перезагрузок (Playwright, iPhone 13 эмуляция, `chromium.launch({executablePath:
  '/opt/pw-browsers/chromium', args:['--use-gl=swiftshader','--enable-webgl',
  '--ignore-gpu-blocklist']})`, `scratchpad/factoryart116/regress.mjs`) — **0** новых ошибок
  консоли на все 10 (штатное предупреждение `navigator.vibrate` исключено из подсчёта явно).
- Старый сейв (`{saveVersion:20,stageIndex:1,money:500,planks:10,concrete:0,metal:0,
  buildings:[{index:0}]}`, ключ `tycoon3d_save_v3`, инъекция через `page.addInitScript`) грузится
  без ошибок: после загрузки `stageIndex=1`, `planks=10`, 1 здание — как и до правки.
- Клиренс конвейер↔дорога и асфальтовая заплатка депо/шпиля (оба из 517fee7/1d57116) — не
  затронуты этим заходом (конвейер и дорожный код не менялись ни на строку), но перепроверены
  напрямую тем же приёмом `Box3.intersectsBox()` по реальным мировым координатам, что и в прошлых
  заходах (`scratchpad/factoryart116/regress.mjs`): **33** меша конвейера (`v116Conveyor`) против
  **12** дорожных мешей (`v59RoadDeck`/`v86StageRoad`/`v66ServiceRoad`) — **0** пересечений;
  инфилл-дек депо/шпиля (`v116SpineGapInfill`) по-прежнему присутствует в сцене.

## 2026-09-26 (четвёртый заход) — Мерцание HUD: найдены и убраны два дублирующих слоя анимации статов

Пользователь сообщил о «мерцании»/дрожании верхнего HUD во время обычной игры (не привязано к
конкретному действию — похоже на постоянный фон). Расследование шло в два неверных предположения
подряд (обе гипотезы честно опровергнуты замерами, не выброшены как «неудобные»), прежде чем
нашёлся настоящий источник.

**Опровергнутая гипотеза №1 — `layoutV46()`'s `v46-tight`.** Переключатель тесного режима HUD
(`tycoon-v116.html`, `layoutV46()`, ~строка 25831) не имел гистерезиса: один и тот же порог 92px
использовался и для входа, и для выхода, а расчёт по размеру окна безусловно сбрасывал класс на
каждый вызов. Теоретически мог дребезжать на пограничном кадре. Замерено дважды: 25 кадров живой
игры (`scratchpad/ui116/out5b.json`) и 60 принудительных тиков в намеренно тесном HUD-состоянии
(`measure6.js`) — **0 переключений в обоих случаях**. Не подтверждено как источник видимого
мерцания, но добавлен гистерезис-барьер как защита на будущее (вход <92px, выход только >108px) —
реальный, измеренный риск дребезга остаётся закрытым, даже если сегодняшнее поведение он не менял.

**Опровергнутая гипотеза №2 — `backdrop-filter: blur()` поверх анимирующейся 3D-сцены.**
Предполагалось, что матовое стекло HUD-панелей постоянно пересэмплирует движущийся фон (машины,
пульсирующее кольцо гид-маркера). Прямой A/B-тест (`scratchpad/ui116/measure7.js`, тот же
реальный игровой сценарий, 29 кадров на конфигурацию, единственная переменная — `backdrop-filter:
none!important` через инжектированный `<style>`) дал **avg 1.58%→1.88%, max 5.63→6.19,
spikesOver2 8→9** — отключение блюра не снизило churn (в пределах шума). Гипотеза отклонена.

**Настоящая причина — два независимых, наслоенных друг на друга слоя «feedback»-анимации статов,
оба реагирующие на одни и те же значения (`money`/`planks`/`concrete`/`metal`/`population`),
добавленные в разные проходы (v47 и v48) и никогда не сведённые воедино:**

- **V47 (`pulseV47`/`snapshotStatsV47`, было ~`tycoon-v116.html:26041-26045`).** При ЛЮБОМ
  изменении значения (даже на 1 юнит) синхронно делал `classList.remove('v47-stat-pulse'); void
  el.offsetWidth; classList.add(...)` — принудительный синхронный reflow на каждый тик, пока
  идёт пассивный доход. Но CSS-переопределение того же прохода v48 (`#hud
  .stat.v47-stat-pulse{animation:none!important}`, было на строке ~26143, стоит позже и с
  `!important` — выигрывает каскад безусловно) **полностью отключало саму анимацию**. То есть JS
  весь этот цикл прогонял вхолостую: ноль визуального эффекта, реальный принудительный reflow
  каждый раз. Комментарий рядом в коде прямо это документировал («v48 replaces it»), но сам
  JV47-код так и не был удалён.
- **V48 (`installMeaningfulHUDFeedback`/`pulseStat`, ~`tycoon-v116.html:26224-26256`).**
  Независимо оборачивал `updateHUD()` ещё раз и запускал ЖИВУЮ анимацию (`translateY(-1px)` +
  30px размытие box-shadow, 0.48s) при изменении статов больше порога. Порог для денег — всего
  `max(18, money*0.004)` (0.4% от текущего баланса), кулдаун — 1100мс на стат. При непрерывном
  пассивном доходе (в тестовом сценарии ~90-200/сек) дельта почти всегда пересекает такой низкий
  порог сразу же, как истекает кулдаун — то есть «осмысленная, ограниченная по частоте» отдача на
  самом деле срабатывала почти непрерывно в обычной игре с текущим доходом, а не только на
  значимые события, для которых она задумывалась.

**Измерено детерминированно** (`scratchpad/ui116/measure_count.js` — `MutationObserver` на
`class` у `#hud .stat`, считает реальные срабатывания за фиксированные 20 реальных секунд
непрерывного дохода, без скриншотов и без их шума по таймингу):
- До фикса: **v47 сработал 59 раз, v48 — 53 раза за 20.5с** (тот же HEAD b95fd1a).
- После: **v47 — 0 раз** (слой полностью удалён), **v48 — 12 раз за 20.5с** (порог поднят
  0.4%→3% баланса, пол 18→40; плоские пороги planks/concrete/metal 1→8, population 3→10;
  кулдаун 1100мс→4000мс) — снижение частоты v48 **на ~77%**.

Также запущен управляемый парный A/B на неизменном коде (`measure8.js`, тот же живой сценарий, 29
кадров на конфигурацию, единственная переменная — CSS `animation:none!important` на
`.v48-stat-feedback`): включено — **avg 2.59%, max 7.18%, 14/29 кадров >2%**; выключено —
**avg 1.10%, max 6.57%, 4/29 кадров >2%** — почти вдвое меньше среднего churn и в 3.5 раза меньше
всплесков >2% при отключении одной только v48-анимации на том же коде.

**Честная оговорка про итоговый before/after по скриншот-based pixel-diff всего HUD-региона**:
отдельные before/after прогоны через новый запуск процесса (не парный A/B на одном коде) дали
противоречивые числа (`out_before2.json` avg 1.46% / `out_final2.json` before avg 0.84% против
after 1.70%/1.67%) — при повторных прогонах ОДНОГО И ТОГО ЖЕ немодифицированного кода
(`measure7`'s `withBlur` avg 1.58% vs `measure8`'s `A` avg 2.59%, тот же код) разброс тоже был
**~3×**. Скриншот-метод под swiftshader-рендерингом слишком шумный по таймингу для чистого
единого числа на выборках 30-60 кадров: правдоподобное объяснение — при непрерывном v48-триггере
(до фикса) анимация почти постоянно «в полёте» и кадр-к-кадру дельта усредняется, а после фикса
редкие, но резкие вспышки (расхождение rest↔glow) дают более контрастные отдельные пики при
меньшей частоте. Детерминированный подсчёт триггеров и управляемый парный A/B — надёжные
подтверждения; необорудованный whole-scenario pixel-diff таким не является в этом окружении и не
противоречит фиксу (числа до/после — в пределах общего шумового разброса, не регрессия).

Тестирование: `node --check` по всем 63 `<script>`-блокам — без ошибок; 10 перезагрузок — 0
новых ошибок консоли; старый сейв (`saveVersion:20`, без ключей v36+) грузится без изменений
(`stageIndex:1`, `buildingsLen:1`, `money≈523` — прирос пассивным доходом за 3.5с загрузки, как и
ожидается); дорожные фиксы последних 5 коммитов переподтверждены вживую
(`roads116/regress_all.js`): `roadOverlap=0`, зазор подъезд↔здание `0`, `junction=(-18,-3.2)`
точно на мастер-сетке, холостой `refreshBaseWorld()` — `0 removed/0 added`.

## 2026-09-26 (пятый заход) — конвейер выглядит неправдоподобно + провал травы в асфальте у лесопилки

Пользователь прислал скриншот телефона с двумя жалобами: «это не реально в жизни, конвейер
который вот такой вот» (мост конвейера из 517fee7 выглядит неправдоподобно) и «ещё не вся дорога
есть повороты, там почему-то багается кусок» (ступенчатая трава прямо в асфальте у перекрёстка).
Методология — Playwright + iPhone 13, порт 8500 (8934/8098 заняты параллельным HUD-агентом на
другом клоне), прямые измерения (`Box3`, raycast по реальным мешам, ноль доверия к
`window.__TYCOON_*__` самоаудитам).

### 1. Провал травы в асфальте у лесопилки (не описанный в ROADS_V116_NOTES пункт «B»)

**Гипотеза-кандидат отклонена.** ROADS_V116_NOTES пункт «B» (`addSideTreatment()` не подрезается
на перекрёстках) и подозрение на регрессию `addCornerArc59()`/`b95fd1a` — обе проверены и
отклонены: живой прогон region-дампа вокруг точки бага (`scratchpad/conveyor116/region_dump.json`)
не нашёл ни одного меша с тегом `v113Decor`/`v59Curb` рядом, а вся геометрия там принадлежит
исключительно системе депо `v66`/`v67`/`v108` (`v67-sawmill-road-surface-final`), не `v59`.

**Корень найден вживую** (raycast сеткой + точечный, `scratchpad/conveyor116/gridmap.js`,
`vertical_probe.js`, `pixel_probe.js` — координаты пикселя со скриншота переведены в мировые через
реальный `camera`/`Raycaster`, не на глаз): дыра — честный **16-точечный** `hole`-контур,
который `roadUnionContours()` (`v94-road-marking-cleanup` → `rebuildRoadUnion()`,
`tycoon-v116.html:32268`) сам корректно репортит как «не покрыто ничем» (проверено временной
инструментацией `window.__DEBUG_UNION_LOOPS__`, снятой после диагностики) — **не баг алгоритма
объединения**, честный отчёт о реальном отсутствии асфальта, bbox `x[-17.17,-14.52]
z[-2.36,-0.91]` (`scratchpad/conveyor116/loops.json`).

**Почему дыра появилась именно сейчас.** `rebuildServiceRoadsV66()` (`v67-sawmill-road-surface-final`,
`tycoon-v116.html:29107`) рисует «public spine» — путь от узла `junction` (-18,-3.2) через
`masterGridPathToHub()`: сначала строго на север вдоль `x=-18` до (-18,0), затем на восток к
хабу (0,0). Депо-кластер (сквозной проезд `sawmillBypass`, погрузочный двор `addLoadingApronV66()`,
развязка `liftBay`) сам доходит на север только примерно до `z=-1.5…-2.4`. До появления «public
spine» (фикс №2 из «третьего захода», `517fee7`) это было обычное открытое поле — ничем не
огорожено, замечаний не вызывало. После него дорога с запада/севера и депо-кластер с юга/востока
образовали правильно замкнутый контур вокруг того же самого незаасфальтированного пятачка —
раньше не бросавшийся в глаза кусок поля стал выглядеть как вырезанная в дороге дыра
(ступенчатая, с закруглённым углом — это внешний контур скруглённого погрузочного двора
`v108RoundedLoadingCourt`, который дыра просто огибает снаружи).

**Фикс** (`rebuildServiceRoadsV66()`, тот же единственный владелец депо-дороги, без нового слоя):
один инфилл-дек, замеренный с запасом за края обеих соседних систем (не даёт новому шву), сразу
после отрисовки «public spine»:
```js
if(hubPath.length>1&&nodes.get('liftBay')&&nodes.get('sawmillBypass')){
  const spineHalfW=legWidth(hubPath[0],hubPath[1])*.5;
  const gapX0=hubPath[0].x+spineHalfW-.15,gapX1=nodes.get('liftBay').p.x-.15;
  const gapZ0=nodes.get('sawmillBypass').p.z+base*.5-.2,gapZ1=hubPath[1].z-spineHalfW+.15;
  if(gapX1>gapX0&&gapZ1>gapZ0){...addDeckV66(...)...}
}
```
Заодно поправлен `roadAuditV66()` (`tycoon-v116.html:29215`): его строгое равенство
`decks.length === <число рёбер графа>` не учитывало новый дек без пары узлов графа и после
фикса честно показывало `false` — добавлен явный `+1` через тег `v116SpineGapInfill` вместо
подгонки лишь бы совпало.

**Тест до/после**: `window.__DEBUG_UNION_LOOPS__` — до фикса `2` контура (`outer` + `16`-точечный
`hole`, площадь дыры `-2.22`); после — **`1`** контур (только `outer`, та же площадь `78.03`,
дыра поглощена). Скриншоты: `scratchpad/conveyor116/apron_topdown.png` /
`apron_low_angle.png` (до, ступенчатая трава видна и сверху, и сбоку) vs
`apron_topdown_after.png` / `apron_low_angle_after.png` (после, сплошной асфальт). Регрессия:
конвейер↔дорога по-прежнему **0** пересечений (см. ниже), `serviceRoadAudit.checks` — все `true`.

### 2. Конвейер выглядит неправдоподобно

**Корень.** `buildSawmillScenery()` (`tycoon-v116.html:2872`), опоры моста строились как `i/7`
для `i=0..7` — 8 точек, разложенных РАВНОМЕРНО по ВСЕЙ длине ленты (`9.53` юнита), большая часть
которой — плоский наземный участок, где опора чисто декоративна. С таким шагом (`~1.36` юнита)
короткий рамповый переход (`0.55` юнита, `CONVEYOR_BRIDGE_RAMP`) целиком помещался в промежуток
между одной низкой (наземной) и одной высокой (мостовой) опорой — измерено вживую: ни один из 8
семплов (`t=1.362`, `2.724`, `4.086`, `5.447`, `6.809`, …) не попадает в диапазон рампы
`[1.55,2.10]`/`[5.60,6.15]`. В результате оба наклонных участка ленты визуально висели в воздухе
консолью от ближайшей высокой опоры — ни одной стойки под самим наклоном, что и читалось как
«так не бывает в жизни» (плюс сами опоры — голая пара вертикальных стоек с одной горизонтальной
перемычкой, без раскосов, то есть не читались как ферма).

**Фикс** (`buildSawmillScenery()`, тот же единственный владелец конвейера, без нового слоя):
опоры вынесены в `addConveyorSupport(worldT)` и переразмещены — 4 редких наземных (как раньше,
`t=0,1/7,6/7,1`) плюс плотный ряд по всему мостовому участку
(`CONVEYOR_BRIDGE_T0-RAMP … CONVEYOR_BRIDGE_T1+RAMP`, шаг `~0.9` юнита, `Math.round`), так что
опора теперь гарантированно стоит у обоих подножий рампы и у обоих переломов уклона. Дополнительно
у каждой опоры с заметной высотой (`elev>0.25`) добавлен диагональный Х-раскос между двумя
стойками (пара наклонных балок, длина/угол считаются из реальной высоты стойки), чтобы секция
моста читалась как настоящая ферма-эстакада, а не пара голых столбов.

**Тест до/после**: `scratchpad/conveyor116/full_side2.png`/`ramp1_close.png`/`ramp2_close.png`
(до — одинокие вертикальные стойки только под плоским верхом моста, рампы без единой опоры) vs
`north_side_wide_after.png`/`north_side_full_after.png`/`final_combined_after.png` (после —
плотный ряд Х-раскосных ферм, рампы обеих сторон стоят на собственных опорах, лента непрерывна
на всех стыках — см. `topdown_full.png`/`seam2.png`, ни одного видимого шва). Регрессия
(реальный `Box3`, не самоотчёт, `scratchpad/conveyor116` inline-скрипт): **0** пересечений
конвейер↔дорога (было `0` и до правки — клиренс из 517fee7 не тронут, `33` меша конвейера против
`2` дека `sawmill-access`).

### Регрессия (оба пункта)

- `node --check` по всем 63 непустым `<script>`-блокам — без ошибок (блоков в файле по-прежнему
  63, новых слоёв не добавлено).
- 10 полных перезагрузок (iPhone 13 эмуляция) — 0 новых ошибок консоли (`navigator.vibrate`
  предупреждение не в счёт).
- Старый сейв (`{saveVersion:20,stageIndex:1,money:500,planks:10,concrete:0,metal:0,
  buildings:[{index:0}]}`, ключ `tycoon3d_save_v3`) загружается без ошибок.
- 5 ранее закрытых пунктов переподтверждены вживую (`scratchpad/conveyor116/regress_all.js`):
  холостой `refreshBaseWorld()` — `0 destroyed/0 created`; `v116FleetYard`↔дорога — `0`
  пересечений; `spawnBuilding(3)` + `window.__TYCOON_STAGE_ROADS__.audit()` — все проверки
  `true`; `window.__TYCOON_V67_ROADS__.run().checks` — все `true` (депо-дорога/апрон/горловина
  на месте, коллизий с коллайдерами нет); 2-полосные повороты не тронуты в этом заходе (другой
  участок кода) и не регрессировали (тот же аудит `v108-all-road-types-polish` проходит).

## 2026-09-26 (третий заход) — Дорога сквозь конвейер, непроложенный путь к стройке, ломаные 2-полосные повороты

Пользователь прислал 3 живых скриншота телефона с новой партией жалоб (плюс четвёртый, во
время работы). Полная методология, числа и скриншоты — `ROADS_V116_NOTES.md`, раздел «Проход
2026-09-26 — третий заход». Здесь — краткая сводка.

**1. Дорога сквозь конвейер лесопилки.** Причина: сегмент депо-дороги `sawmillBypass↔depotWest`
буквально пересекает конвейерную ленту (общий OBB-тест, не наивный AABB) — глубина `1.5`/`1.12`.
Геометрический анализ доказал, что объезд невозможен без выезда в коллайдер здания (оба конца
сегмента лежат по разные стороны от оси конвейера, глубоко внутри его длины). Фикс — приподнять
сам конвейер в мост ровно на измеренном участке пересечения (`buildSawmillScenery()`, новая
`addConveyorSpanBox()`/`conveyorElevationAt()`). После: **0** реальных 3D-пересечений (было 2,
полное перекрытие и по XZ, и по высоте); зазор под мостом — `1.32` юнита, больше высоты грузовика.

**2. Нет асфальта от источника до стройки.** Реальный маршрут грузовика был покрыт асфальтом на
`93.6%`, с честным `6.11`-юнитным разрывом без единого меша рядом. Причина: `buildRoute()`
(v86) вело конец маршрута из депо через поиск «ближайшей точки на маршруте активных домов»,
которая не знает про мастер-сетку и может уйти в сторону; сама мастер-сетка при этом — только
логическая рамка, реальный асфальт по ней рисуется лишь когда открыт хотя бы один район (в
типичном раннем состоянии — их 0). Фикс: новая `masterGridPathToHub()` заменяет этот поиск
обходом всегда-определённого графа мастер-сетки; тот же путь также нарисован как настоящий,
всегда включённый асфальт («public spine») в `rebuildServiceRoadsV66()`. После: **100%**
покрытие, **0** неасфальтированных юнитов, воспроизведено на ближней и дальней стройке.

**3. Повороты 2-полосных дорог ломаются.** Реальный CORNER-перекрёсток давал зубчатый 12-точечный
контур (звездообразная огибающая объединения прямоугольников, `roadJunctionOutlineXZ()`) с
разрывом касательной `90°` у самого стыка — вместо гладкой дуги окружности, которой рисуются
однополосные STAGE-повороты (`addCorner()`, v86) и которую пользователь назвал «просто блеск».
Фикс, тот же геометрический принцип применён к собственному коду v59 (не силовое объединение баз
— v59 ещё рисует бордюр/тротуар, которых у v86 нет): новая `addCornerArc59()` строит ту же дугу
и достраивает вдоль неё бордюр/тротуар; попутно найден и исправлен зависимый баг калибровки
отступа (`jointHalf59()`), из-за которого дуга математически не помещалась при типичной ширине
2-полосной дороги и код тихо откатывался на старую зубчатую заливку. После: гладкая 25-точечная
дуга, разрыв касательной `1.88°` (дискретизация хордами, не структурный излом).

**4. «Проплешина на углу, которая сама чинится».** Расследовано вживую (замеры через 0–1000мс
после спавна новой далёкой стройки) — отдельного, самостоятельного бага не поймано; во всех
точках замера сеть уже полностью собрана. Наиболее вероятное объяснение — тот же баг, что и
пункт 2, пойманный пользователем до срабатывания фонового `refresh()`; должен быть закрыт тем же
фиксом.

**Пункт 5 (перестановка зданий)** — не сделано в этом заходе, сознательно (весь бюджет ушёл на
пункты 1–4); координаты производственных зданий не менялись.

Тестирование: 10 перезагрузок — 0 новых ошибок; старый сейв грузится; все 4 фикса предыдущих
заходов переподтверждены вживую после всех правок; все 63 script-блока валидны.

## 2026-09-26 (второй заход) — Кривой стык у Лесопилки, рецидив «дорога перекладывается сама», полный повторный аудит

Пользователь прислал живой скриншот телефона с конкретным кривым стыком у Лесопилки и спросил
честно: «ты уже все дороги проверил на все критерии, а не только мои три пункта?». Ответ — нет,
не все; в этом заходе — да. Полная методология, числа и найденные-но-не-исправленные пункты —
в `ROADS_V116_NOTES.md`, раздел «Проход 2026-09-26 — второй заход». Здесь — краткая сводка.

**1. Кривой стык (конкретная жалоба со скриншотом).** Причина: `serviceAccessGraph()`
(`tycoon-v116.html`, сеть подъездов к промышленному кластеру) вычисляла точку выхода на
«настоящую дорогу» через глобальную `projectToRoad()` — но эту функцию позже переопределяет
`v86-canonical-stage-roads` на проекцию по STAGE-маршрутам игрока (нужно для навигации машин).
Без активных stage-дорог рядом это давало `(0,0,0)` — мировой хаб, и депо-дорога превращалась
в шальную диагональ **14.17** юнита через открытое поле/хаб, пересекая настоящую дорогу игрока
**2 раза** с изломом касательной **13.1°** и обрываясь в воздухе (тот самый разрыв асфальта с
висящими обрезками разметки на скриншоте). Фикс: новая `projectToMasterRoadGridV116()`
проецирует на всегда-существующую логическую рамку города (`masterRoadSegmentsV362()`) вместо
волатильных stage-маршрутов. После: узел на `(-18.00,-3.20)`, сегмент **4.2** юнита,
**0** пересечений с реальной дорогой, **0** столкновений с домами в радиусе 22 юнитов.

**2. Рецидив «дорога перекладывается сама».** Отдельная, ранее непотроганная точка входа
(`window.__TYCOON_V61_AUDIT__.refresh`, зашитая на сырой `v86.refresh()`) вызывалась
безусловно из фонового `fullSync()` на каждый тик `refreshBaseWorld()`/
`refreshSpecialProjectWorld()` — то есть полный снос+пересборка всей сети stage-дорог
происходили примерно раз в 2 секунды в чистом простое, без единого действия игрока. Фикс —
сигнатура-гвард на существующем счётчике `generation` у `v86` прямо в точке вызова
`fullSync()`. После: 0/0 на 5 холостых тиках подряд (было 7/7 на каждом); официальный
регрессионный тест проекта (`diag1.js`) — 0/0 на всех сценариях, 132 kept; новое здание
по-прежнему честно добавляет дорогу.

**3. Системный повторный аудит.** 15 категорий по методологии `TYCOON_V62_AUDIT.md`
«Проход 3» — таблица с числами в `ROADS_V116_NOTES.md`. Найдено и задокументировано (не
исправлено — риск для декоративного слоя с несколькими вызывающими местами превышает пользу
в рамках одного захода): (A) районные улицы всех 4 районов рисуются старым «кольцевым»
дизайном вместо описанной в комментариях прямоугольной сетки — вторая версия
`makeCityStreetGroup` физически недостижима (объявлена `function`-ом внутри чужого IIFE);
(B) декоративный бордюр/тротуар мастер-сетки (`addSideTreatment`) не обрезается перед
перекрёстком и в 3 местах реально проходит сквозь развязку.

Тестирование: 10 перезагрузок — 0 новых ошибок; старый сейв грузится; все 3 фикса первого
захода (9a5c090) переподтверждены реальными измерениями; все 63 script-блока валидны.

## 2026-09-26 — Дорожная сеть: единый источник истины для разметки, конец дрейфа парковки, подъезды впритык

Прямой запрос пользователя (перевод с русского): сделать нормальную дорожную сеть — отдельные
подъезды к зданиям, корректно подключённые к главной дороге; главное требование — дорога
должна быть единой сетью, которая **не перекладывается заново после каждого апгрейда**; и
дорога должна перестать пересекать парковку для транспорта. Все три пункта закрыты: первые два
— в первом заходе (ниже), третий («подъезды впритык») — во втором, отдельном заходе того же
дня после независимой проверки первого (раздел 3).

`tycoon-v116.html` — импортированный отдельный файл (та же линия, что и `tycoon-v63.html`, но
независимо развившаяся дальше: между v78 и v116 добавлено 38 новых дорожных патч-слоёв —
`v98-real-road-network` … `v116-final-road-audit`). `tycoon-v63.html`/`tycoon-v62.html`/
`tycoon-v55.html` не тронуты. Инструменты: Playwright (iPhone 13 эмуляция,
`chromium.launch({ executablePath: '/opt/pw-browsers/chromium', args: ['--use-gl=swiftshader',
'--enable-webgl', '--ignore-gpu-blocklist'] })`), `python3 -m http.server` из корня репозитория;
скрипты/сырые данные/скриншоты — `scratchpad/roads116/` (не закоммичены).

**Важная методологическая находка.** Файл уже содержит систему `v113-stable-road-blueprint`
(`window.__TYCOON_V113_ROADS__`), которая на вид решает ровно эту задачу: она перехватывает
глобальные `refreshLivingDistrictStreets`/`refreshArterialRoads`/`refreshDistrictAccessRoads`/
`refreshTransportAccessRoads`/`refreshStageRoadsV367`/`refreshDrivewaysV35` и подменяет их на
инкрементальные `syncX()`-версии, которые не сносят сеть целиком. Её собственный аудит
(`window.__TYCOON_V113_ROADS__.audit().checks.noGlobalRuntimeRebuilds`) всегда возвращал `true`.
Как и предупреждал `TYCOON_V62_AUDIT.md` (Проход 4) про `v57`/`v58`, это самосверяющийся аудит:
он проверяет счётчик, который сам же ведёт, а не реальную геометрию. Независимая проверка
(diff мешей по `uuid` до/после реального клика по кнопке апгрейда, плюс перехват
`THREE.Object3D.prototype.remove`) показала, что `v113` действительно перестала пересобирать
асфальт/бордюры/тротуары — но **разметка** (`window.__TYCOON_V94_MARKINGS__`, отдельный,
более старый владелец) как пересобиралась целиком на каждый вызов, так и продолжала это делать
после захвата `v113` — потому что `v113` сама же безусловно вызывает
`window.__TYCOON_V94_MARKINGS__.apply()` в конце каждого `syncAll()`.

### 1. «Дорога перекладывается после каждого апгрейда» — реальный источник: разметка, не асфальт

Место: `apply()` в `v94-road-marking-cleanup` (`tycoon-v116.html:32106`, до фикса). Функция на
каждый вызов делала `hideLegacy`+`normalizeAsphalt`+`rebuildRoadUnion` (у него уже был свой
`lastUnionSignature`-кэш, поэтому асфальт не пересобирался) и затем **безусловно** для каждого
корня `addDashes(root)` → `removeLayer(root)` (снос группы `v95UnifiedMarkings` с dispose
геометрии/материалов) → пересоздание всех дэшей заново. Никакой проверки «а изменилось ли
что-то» не было — единственный владелец разметки полностью пересоздавал её на каждый вызов.

`apply()` вызывается из `syncAll()` (`v113`, `tycoon-v116.html:33937` до фикса) на **каждом**
`refreshInfrastructureWorldV37()`, а он, в свою очередь, срабатывает при завершении таймера
любого апгрейда дороги/освещения района (`tycoon-v116.html:10191`/`10179`) — то есть на каждый
клик «Улучшить дорогу»/«Улучшить освещение» вся белая разметка по всей карте демонтировалась и
рисовалась заново. Визуально это неотличимо от «дорогу перекладывают».

**Тест до фикса** (`scratchpad/roads116/diag1.js`, снимок мешей с тегами
`v59RoadDeck/v86StageRoad/v66ServiceRoad/v116PersistentRoad/.../v95UnifiedDash` по `uuid` до/
после реального клика): апгрейд дороги района (клик `[data-district-roads="suburb"]`) —
**37 уничтожено / 37 создано** (полный набор дэшей); апгрейд освещения другого района — 37/37;
`upgradeCityDistrict('waterfront')` (тот же код, что за кнопкой) — 37/37; и даже **полностью
не связанный апгрейд гаража автопарка** (`buyBaseUpgrade('garage')`) — тоже 37/37. Изолированный
холостой прогон без единого действия (`diag_idle.js`, 6 тиков по 1.5с) — 0/0, то есть проблема
не в фоновом таймере, а именно в том, что любое из этих действий доходит до `apply()`.

Фикс (`tycoon-v116.html:32106`, тот же единственный владелец, без нового патч-слоя): добавлена
`applyInputSignature()` — тот же приём, что уже используют `serviceGraphSignature()`/
`drivewaySig()` в этом файле (сигнатура по `uuid`+`visible` реальных дорожных мешей + условие
покрытия дорог по районам), и `apply()` возвращает закэшированный результат, если сигнатура не
изменилась. Реальное изменение сети (новый мост, новый уровень дороги, новый штрих) меняет
набор `uuid` или условие → полный пересчёт по-прежнему происходит; повторный вызов с тем же
состоянием — нет.

**Тест после фикса** (тот же `diag1.js`): апгрейд дороги района — **0/0** (86 сохранено);
апгрейд освещения — 0/0; `upgradeCityDistrict` (район реально поднялся с L2 на L3, разметка не
пересобиралась, но легитимно **добавились** 34 новых меша — бордюр/тротуар/переход L3,
уничтожено 0); несвязанный апгрейд гаража — 0/0. То есть: разметка больше не трогается там, где
ничего не изменилось, и по-прежнему честно достраивается там, где что-то реально появилось.

### 2. «Дорога пересекает парковку» — реальный источник: парковку сдвигает чужая система очистки меток

Место: `run()` в `v102-label-and-road-clearance` (`tycoon-v116.html:32538`, до фикса). Проход
предназначен для мелких UI-подсказок («автопарк», «команда», «город» — плашки-кружки над
кнопками мира) — он ищет группы, чей текст похож на подпись, и через `nudgeGroup()` сдвигает их
подальше от дороги. Ловушка: критерий матчинга был `(labelText || '') + ' ' + (obj.name || '')`,
а НЕ только текст подписи. Реальная группа автопарка (`v116FleetYard`, вся стоянка со всеми
боксами/линиями разметки — `tycoon-v116.html:28537`, слой `v65-depot-traffic-queue-fix`) так и
называется — регэксп `/Fleet/i` ловил её саму, целиком, как будто это маленькая UI-плашка.

`nudgeGroup()` читает `group.getWorldPosition()` — то есть позицию САМОЙ группы (у
`v116FleetYard` она равна `(0,0,0)`, потому что все дочерние меши несут собственные абсолютные
локальные координаты вроде `(-8.70, ..., 6.72)`, а сама группа-контейнер не смещена). `(0,0,0)`
физически лежит на центральной магистрали (`masterRoadSegmentsV362`, ось X=0). Функция честно
нашла «UI-плашку в 0 юнитах от дороги» и оттолкнула её на `1.34` — но оттолкнула **всю стоянку
целиком**, а не что-то маленькое. Проход перезапускается после каждого `stageApi.refresh()`
(3 раза, `+80/+420/+1100мс`) плюс стартовые прогоны — за несколько последовательных пересборок
смещение накопилось.

**Тест до фикса** (`scratchpad/roads116/diag_fleet.js`): `scene.getObjectByName('v116FleetYard').
position` = `(4.02, 0, 0)` (должно быть `(0,0,0)`); плита стоянки (меш `lot`,
`BoxGeometry(11.92,...,3.72)`, автор задумывал центр `x=-8.70`) реально стояла в мире с
`Box3 = {min:[-10.64,...], max:[1.28,...]}` — то есть кабинет стоянки заехал почти до `x=1.28`.
Реальное пересечение `Box3.intersectsBox()` с сегментом магистрали `arterial-master`
(`x∈[-1.24,1.24]`) — **1 пересечение** (не самоотчёт файла, а честный тест бокс-в-бокс на
фактических мировых координатах после `updateWorldMatrix`).

Фикс (`tycoon-v116.html:32538`, тот же владелец, без нового слоя): критерий сузили до **только**
`labelText` (реальный текст подписи, не внутреннее имя объекта) и добавили явное исключение
известных инфраструктурных корней (`v116FleetYard`, `cityWorldRuntime.stageRoads/masterRoads/
transportAccessRoads`) — они никогда не должны рассматриваться как «маленькая UI-плашка».
Дополнительно в `rebuildParkingV65()` (`tycoon-v116.html:28537`) явное
`g.position.set(0,0,0)` с комментарием — защита от повторения той же ошибки другим будущим
патчем, который случайно заматчит имя.

**Тест после фикса**: `v116FleetYard.position` = `(0,0,0)`; плита стоянки —
`Box3 = {min:[-14.66,4.86,...], max:[-2.74,8.58,...]}`, полностью в стороне от `x∈[-1.24,1.24]`
магистрали; **0 пересечений** `Box3.intersectsBox()` по всем реальным дорожным мешам
(`v59RoadDeck`/`v86StageRoad`/`v66ServiceRoad`/`v116PersistentRoad`). Скриншот
(`scratchpad/roads116/after_fleetyard_topdown.png`, вид сверху) визуально подтверждает: серая
стоянка с разметкой боксов отделена травой от чёрной дороги, наезда нет.

### 3. «Отдельные подъезды к зданиям» — реальный источник: два независимых определения «края здания» разошлись на 1.7 юнита

Первый заход (см. предыдущую запись этого changelog и `ROADS_V116_NOTES.md`) измерил, но не
исправил: подъездные меши с ролью `stage-driveway` реально существуют и подключают здания к
сети, но типичный зазор от края застройки до дороги/подъезда был `~1.8-3.5` юнита вместо `~0`.
Этот заход находит и закрывает точную причину.

Место: `stageRoadEndpointV367()` (`tycoon-v116.html:15338`, до фикса). Эта функция — ЕДИНАЯ
точка, в которой физически заканчивается асфальт последнего сегмента дороги для stage-здания:
её использует и канонический маршрутизатор `plan()` в `v86-canonical-stage-roads`
(`tycoon-v116.html:31356`, `finalEnd=stageRoadEndpointV367(i)`, попадает в путь на
`tycoon-v116.html:31393`), и bootstrap-фоллбэк `stageRoadPolylineV367`
(`tycoon-v116.html:15361`) — то есть какая бы из двух систем ни оказалась «живой» на момент
рендера, обе кладут асфальт ровно до этой точки. Она же задаёт позицию декоративного pull-in
bay (`stageDrivewayGeometryV351.bay`, `tycoon-v116.html:13679`, использует
`stageRoadEndpointV367` напрямую) и точку, куда едут служебные машины
(`worksiteVehicleApproach`, `tycoon-v116.html:13684`).

До фикса `stageRoadEndpointV367` откладывала от здания `радиус_коллизии + 1.82` юнита. Проблема
в том, что в этом же файле, в той же самой функции-соседке (`stageDrivewayGeometryV351.edge`,
`tycoon-v116.html:13678`), уже посчитан «настоящий край» здания — `радиус_коллизии + .12`. Это
разные конкретные точки на карте, отличающиеся ровно на `1.82-.12=1.70` юнита: одна («true
edge») используется для аудита/логистики, другая («road endpoint») — для того, где физически
кончается асфальт. Комментарий на месте старого кода объяснял `+1.82` желанием «оттащить рот
подъезда подальше, чтобы дорога не заезжала визуально в дома» — но это рассуждение появилось
до того, как `stageAccessRadiusV92()` стал брать радиус из `buildingCollisionRadius()` (реальный
архетип здания, `stage.baseSize*.64+...`), то есть до того, как радиус вообще стал точным. Раз
радиус уже равен настоящему футпринту, добавочные 1.7 юнита сверху — это просто трава, которую
асфальт больше не покрывает, а комментарий `v35.3` в `refreshDrivewaysAndApronsV353`
(`tycoon-v116.html:13976`) прямым текстом запрещает рисовать здесь второй, дублирующий слой
асфальта («V86 is the only owner of the stage driveway asphalt») — то есть ничего другого этот
разрыв заполнить и не могло.

**Тест до фикса** (`scratchpad/roads116/diag_driveway2.js`; здания через
`spawnBuilding(index,1,{grow:false})` по проектной конвенции; зазор считается от НАСТОЯЩЕГО
края здания — `buildingCollisionRadius(b)+.12` — до `Box3` ближайшего меша с флагом
`v59RoadDeck`/`v86StageRoad`/etc., а не от общего `BUILDING_RADIUS`, как в черновом
`diag_driveway.js` из первого захода): здания 1-4 — зазор `~1.67` юнита стабильно (1.670,
1.670, 1.670, 1.670); здание 0 (уже у хаба) — `-0.845` (касание, было и раньше нормально).

Фикс (`tycoon-v116.html:15338`, тот же единственный владелец, без нового слоя): отступ сужен
с `+1.82` до `+.12` — то есть теперь он в точности совпадает с формулой «настоящего края»,
которую уже использует соседняя `stageDrivewayGeometryV351.edge`. Обе системы, дающие асфальт
(canonical и fallback), и pull-in bay/точка подъезда машин сдвигаются согласованно, потому что
все они читают одну и ту же функцию — отдельного «шарящего» коннектора писать не пришлось,
система уже была единой, просто с рассинхронизированной константой. Заодно обновлён устаревший
порог «bay too close» в `auditV352AccessNetwork()` (`tycoon-v116.html:13886`): он был
`радиус+.45` — откалиброван под старый баг и физически не мог поймать разрыв в 1.7 юнита
(проверял только нижнюю границу «слишком близко», а не верхнюю), теперь `радиус+.05`, чтобы
по-прежнему ловить регресс в другую сторону (bay внутри здания), но не шуметь на новый,
намеренно тесный дизайн.

**Тест после фикса** (тот же `diag_driveway2.js`): здания 1-4 — зазор `~-0.03` юнита (полотно
и pull-in bay касаются/чуть перекрывают настоящий край здания вместо `1.67` юнита травы);
здание 0 — `-0.978` (без изменений по сути, было и осталось впритык). Скриншоты
(`scratchpad/roads116/after_driveway_building3.png`, `after_driveway_building1_topdown.png`)
визуально подтверждают: асфальт с белой разметкой доходит прямо до фундамента здания, видимого
разрыва травы между дорогой и постройкой больше нет. Дополнительно прогнан
`window.__TYCOON_STAGE_ROADS__.audit()` после спавна 8 stage-зданий
(`scratchpad/roads116/audit_check.js`): `stageRoadAssetCollisions:[]`, `checks.routesClear:true`
— более тесный отступ не заводит маршрут внутрь коллайдера самого здания и не создаёт новых
коллизий с другими объектами; `auditV352AccessNetwork()` — `issues:[]` (новый порог `+.05` не
шумит на намеренно более тесный дизайн).

### Тестирование фиксов 1-3

- `node --check` по регэкспу `<script(?![^>]*src)[^>]*>(.*?)</script>` (DOTALL) — все 63 блока
  по-прежнему парсятся (перепроверено после фикса подъездов).
- 10 полных перезагрузок (iPhone 13 эмуляция) — 0 новых ошибок консоли/`pageerror` (не считая
  штатного предупреждения `navigator.vibrate` без тапа).
- Старый сейв (`{saveVersion:20, stageIndex:1, money:500, planks:10, concrete:0, metal:0,
  buildings:[{index:0}]}` под `SAVE_KEY='tycoon3d_save_v3'`, инъекция через
  `page.addInitScript`) загружается без ошибок: `stageIndex=1`, `planks=10`, 1 здание — как и
  до фиксов (формат сейва не менялся, `saveVersion` не менялся).
- Служебные машины (`serviceVehicles`) по-прежнему существуют и позиционируются относительно
  дорожной/парковочной сети (`scratchpad/roads116/vehicle_check.js`) — фикс подъездов не
  трогал код движения/AI, только точку, где заканчивается асфальт stage-здания.
- Регрессия фиксов 1-2 перепроверена в этом же заходе, после фикса подъездов, через тот же
  `diag1.js`: апгрейд дороги района — `0/0` (86 сохранено); апгрейд освещения (другой район,
  первый реальный подъём уровня) — `0 destroyed`/`12 created` (легитимные новые фонари, не
  разметка); `upgradeCityDistrict('waterfront')` — `0 destroyed`/`34 created` (легитимный
  бордюр/тротуар нового уровня); несвязанный `buyBaseUpgrade('garage')` — `0/0`;
  `parkingOverlap.overlapCount:0`; `consoleErrors:[]`; перехват `Object3D.prototype.remove`
  по дорожным мешам — `0` срабатываний. Оба фикса из 9a5c090 не сломаны.
