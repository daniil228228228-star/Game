# Changelog — Tycoon v31 (City Expansion & Strategy)

## 2026-09-20 — Импорт файла + аудит + roadmap

- Импортирован присланный пользователем файл (29.5MB, изначально 10915 строк) как
  новая актуальная линия разработки, заменяющая v19 в приоритете.
- Вынесены встроенные ассеты (`assets_v31/*.jpg|png`, идентичны ассетам v19 побайтово)
  и Three.js r128 + OrbitControls (`vendor_v31/three_r128/`) из inline `<script>` в
  отдельные файлы — итоговый `tycoon-v31.html` сокращён до 9849 строк.
  По пути была допущена и исправлена ошибка экстракции (нечёткий поиск `<script>`-тега
  зацепил не тот блок и осиротил код boot-error-хендлера как видимый текст страницы) —
  исправлено переходом на точные, проверенные через `grep -n`, номера строк вместо
  эвристики. Проверено скриншот-сравнением до/после с оригиналом — идентично.
- Полный аудит файла (все 9849 строк) против 23-пунктового видения пользователя
  проведён через отдельного subagent-а. Результат: слой «строительная компания»
  (парк техники, база, контракты, тендеры, репутация) уже сделан хорошо и глубоко.
  Слой «город как живой организм» (население, районы-как-симуляция, гражданская
  инфраструктура с эффектом, стоимость земли, ветвление застройки, гражданская жизнь,
  растущая дорожная сеть) практически отсутствует — есть только именной скелет
  (`CITY_DISTRICTS`/`CITY_MARKETS`/`cityState`) без реальных данных симуляции.
- Составлен и отправлен пользователю roadmap из этапов A–G (жилой город →
  инфраструктура → дороги/трафик → экономика города → девелоперская стратегия →
  живой мир → полировка). Полный текст — в `TYCOON_V31_PLAN.md`.
- Коммит `3660ebd`: файл + ассеты + vendor. Документация (этот файл + план) —
  следующим коммитом.

## 2026-09-20 — Этап A, срез 1: «жилой город» — население как ресурс, первое жилое здание, районы с реальными статами

- **Население (`population`)** стало настоящим отслеживаемым ресурсом:
  `cityState.population` хранится в сейве (как `money`/`planks`/`concrete`),
  показывается в HUD новым чипом `🏘️ N/CAP` рядом с деньгами/досками/бетоном/
  металлом. Растёт/убывает простым lerp'ом к вместимости жилья
  (`updatePopulation()`: `population += (capacity - population) * 0.15`),
  тикает раз в секунду вместе с остальной городской симуляцией (переиспользован
  существующий `cityTickAccumulator` в `updateCityExpansion()`, никакой новой
  per-frame логики) — намеренно без модели спроса/предложения, это задел для
  этапа D.
- **Первое настоящее жилое здание**, отдельное от производственной спирали
  `STAGES`: `RESIDENTIAL_BUILDING` («Жилой дом» / Apartment Block, +60 к
  вместимости населения). Строится через панель «Город и развитие»
  (`renderCityPanel()`), кнопка в карточке района «Жилой район» — стоит
  650 💰 + 4 🪵, 4.5с стройки. Визуально стройка идёт по-настоящему: вызывается
  тот же `spawnConstructionSite()`, что и для зданий спирали (кран, экскаватор,
  бетономешалка), но НЕ регистрируется в `growingMeshes`/`STAGES`-индексации —
  рig анимируется и сам себя убирает через уже существующий
  `updateConstructionSites()` (он умеет работать без `buildRef`), поэтому
  никакой код спирали не тронут. По завершении на сцену добавляется настоящее
  здание через переиспользованный `createBuildingMesh()` (archetype `office`,
  тёплый терракотовый цвет вместо офисного синего стекла — с балконами).
  Место под здание ищется рядом с хабом района «suburb» с обязательной
  проверкой `distToNearestRoadSegment()` (та же защита от «декор/здание легло
  на дорогу», что уже используется для рассыпанного по карте декора) —
  подтверждено численно в тесте (см. ниже), а не только скриншотом.
- **`CITY_DISTRICTS` получили реальные статы** вместо плоского
  unlock-уровня: `cityState.districtStats[id] = { population, satisfaction }`
  — 2 числа на район, обновляются раз в секунду в новой `updateDistrictStats()`
  от уровня района (существующий механизм апгрейда) и факта постройки жилья;
  не скрытая тяжёлая симуляция, просто сглаживание к цели. Показывается в
  каждой карточке района в панели «Город и развитие», на ru и en.
- `saveVersion` 19 → 20. В `load()` добавлена миграция: у старых сейвов без
  `cityState.population`/`.housing`/`.districtStats` эти поля получают
  дефолты (0 / `{apartmentBlockBuilt:false}` / `{population:0,satisfaction:50}`)
  — без падений, по тому же паттерну, что и остальные поля `cityState`.
- Не тронуто: `STAGES`, `growingMeshes`, `constructionQueue`, pad-механика
  основной спирали построек, весь слой fleet/base/contracts/tenders/
  `COMPANY_TIERS`/`CLIENT_TIERS` — только читались как референс.
- Проверено headless Chromium через Playwright (`chromium.launch` с
  `--use-gl=swiftshader`, вьюпорт 900×600): чистый boot без консольных
  ошибок; постройка жилого дома → чип населения растёт от 0 к 60 за
  несколько секунд; численная проверка клирэнса (`THREE.Box3` + четыре угла
  бокса здания через `distToNearestRoadSegment()`) — минимальное расстояние
  до дороги ~24 (порог 2.6, дом стоит у suburb-хаба в углу карты, далеко от
  спирали дорог); старый сейв (сконструирован вручную, `saveVersion: 19`, без
  `population`/`housing`/`districtStats`) загружается без ошибок с
  корректными дефолтами и сохранённым старым прогрессом (деньги, репутация,
  уровни районов); апгрейд района и запуск городской стратегической
  инициативы (существующие, нетронутые механики) по-прежнему работают;
  переключение языка ru/en пересобирает подписи жилого здания без ошибок.

## 2026-09-20 — Этап A, срез 2: «жилой город» расширен на несколько районов

- **Жильё стало доступно в нескольких районах**, не только в suburb:
  `cityState.housing` из единственного плоского объекта среза 1 стал объектом
  по районам — `{ suburb, business, waterfront }`, каждый со своим
  `{apartmentBlockBuilt, pendingUntil, pos}`. `industrial` намеренно без
  жилья (тематически промзона, не жильё). Выбран вариант (a) из ТЗ —
  несколько экземпляров существующего `RESIDENTIAL_BUILDING`, а не второй
  тир апгрейда — прямое обобщение конвейера среза 1 вместо второй системы.
  Кнопка «Построить жилой дом» теперь есть в карточке любого из трёх районов
  панели «Город и развитие», как только район разблокирован (та же
  `districtUnlocked()`), поэтому за игру реально строится до 3 отдельных
  видимых 3D-зданий вместо одного.
- **Размещение** каждого нового здания ищется обобщённой версией функции
  среза 1 — `residentialBuildingPosition(districtId)`, кольцевой перебор
  вокруг хаба конкретного района с той же проверкой
  `distToNearestRoadSegment()`. Добавлена проверка на непересечение
  жилых зданий друг с другом (`BUILDING_CLEAR = 6`) — простая дистанционная
  проверка по образцу уже существующего decor-скаттера против
  `stagePositions` (`createGroundMicroDetail()`).
- **Население расширено на 2 района**: `updateDistrictStats()` теперь лерпит
  население отдельно для каждого района с построенным жильём
  (`suburb`/`business`/`waterfront`) тем же коэффициентом 0.15/с, что и в
  срезе 1 (был только suburb). `cityState.population` (HUD-чип) — сумма по
  всем районам. `cityState.districtStats[id]` трёх районов показывает
  реальное различное ненулевое население после постройки жилья в каждом,
  видно в панели «Город и развитие» на ru/en.
- Видимый рост города — сами 2-3 дополнительных жилых здания в разных углах
  карты, без дополнительных визуальных систем (например, подсветки окон)
  — осознанно достаточно для этого среза.
- `saveVersion` 20 → 21. В `load()` добавлена миграция: сейв `saveVersion`
  20 с плоским `cityState.housing` (`{apartmentBlockBuilt,pendingUntil,pos}`,
  всегда про suburb) превращается в `housing.suburb`, `business`/
  `waterfront` получают дефолт «не построено»; более старые сейвы без поля
  `cityState.housing` вовсе получают дефолт для всех трёх районов — по
  тому же паттерну, что и миграция среза 1.
- Не тронуто: `STAGES`, `growingMeshes`, `constructionQueue`, pad-механика
  основной спирали построек, весь слой fleet/base/contracts/tenders/
  `COMPANY_TIERS`/`CLIENT_TIERS` — только читались как референс. Тик
  по-прежнему раз в секунду через существующий `cityTickAccumulator`
  (`updateCityExpansion()`), никакой новой per-frame логики.
- Проверено headless Chromium через Playwright (`chromium.launch` с
  `--use-gl=swiftshader`): чистый boot без консольных ошибок; постройка
  жилья в suburb/business/waterfront → население всех трёх растёт
  независимо к своей вместимости (60 каждое, 180 суммарно); численная
  проверка клирэнса (`THREE.Box3` + углы + центр через
  `distToNearestRoadSegment()`) для всех трёх зданий — минимум ~3.4-24
  (порог 2.6); численная проверка непересечения зданий друг с другом —
  минимум ~72 (порог 6); старый сейв со сплюснутым `housing`
  (сконструирован вручную, `saveVersion: 20`, точно в формате среза 1)
  загружается без ошибок, мигрирует в `housing.suburb`, business/waterfront
  — дефолт; ещё более старый сейв без `cityState.housing` вовсе
  (`saveVersion: 19`) — все три района дефолт, 0 населения, без падений;
  переключение языка ru/en пересобирает все построенные жилые здания и их
  подписи без ошибок; панели fleet/base/tenders/contracts/systems/city
  по-прежнему рендерятся без ошибок (`renderFleetPanel`, `renderBasePanel`,
  `renderTendersPanel`, `renderContracts`, `renderSystemsPanel`,
  `renderCityPanel`, `renderOperationsPanel`, `renderFleetOperations`,
  `renderStaffPanel`).

## 2026-09-20 — Этап B, срез 1: «городская инфраструктура» — первое гражданское здание (поликлиника)

- **Первое гражданское здание этапа B**: `CLINIC_BUILDING` («Поликлиника» /
  Clinic, `⚕️`), отдельная от жилья (`RESIDENTIAL_BUILDING`) система,
  переиспользующая тот же конвейер строительства. Строится через панель
  «Город и развитие» (кнопка «Построить поликлинику» в карточке района) —
  стоит 900 💰 + 3 🪵 + 2 🧱, 5.5с стройки через переиспользованный
  `spawnConstructionSite()`, настоящий 3D-меш через `createBuildingMesh()`
  (archetype `shop`, кремовый цвет `0xf1ece2` — визуально отличим и от
  терракотового жилого дома, и от заводов промзоны). В отличие от жилья,
  поликлинику можно построить **в любом из 4 районов, включая `industrial`**
  (`CIVIC_DISTRICT_IDS` = все районы CITY_DISTRICTS) — у промзоны раньше не
  было собственных зданий вовсе, заводская поликлиника для рабочих — их
  первая постройка.
- **Реальный игровой эффект**: `cityState.districtStats[id]` получил новое
  поле `health` (0-100), считается в той же `updateDistrictStats()` — база от
  уровня района, +42 к цели при построенной поликлинике, лерп 0.2/тик (тот же
  паттерн, что и `satisfaction`). Поликлиника также поднимает цель
  `satisfaction` (+16) и **ускоряет рост населения**: коэффициент lerp'а
  населения к вместимости жилья вырос с 0.15/с до 0.225/с (×1.5) в районах с
  собственной поликлиникой — простая модель близости «тот же район», без
  пространственной симуляции. Видно в карточке района панели «Город и
  развитие» (`👥 N · Довольство X% · Здоровье Y%`) и в подписи здания на
  карте.
- **Размещение**: `civicBuildingPosition(districtId)` — тот же кольцевой
  перебор вокруг хаба района, что у `residentialBuildingPosition()`
  (`distToNearestRoadSegment()` для дорог), со сдвинутым углом перебора и
  обобщённой проверкой непересечения `occupiedBuildingPositions()`
  (объединяет позиции всех построенных/строящихся жилых И гражданских
  зданий) с тем же `BUILDING_CLEAR = 6.0`.
- `saveVersion` 21 → 22. В `load()` добавлена миграция: `cityState.civic`
  (отсутствует целиком у старых сейвов) — все 4 района получают дефолт «не
  построено»; `districtStats[id].health` (отсутствует) — дефолт 50. Остальные
  поля (`housing`, `population`, `districtStats.population`) сохраняются как
  были.
- Не тронуто: `STAGES`, `growingMeshes`, `constructionQueue`, pad-механика
  основной спирали построек, весь слой fleet/base/contracts/tenders/
  `COMPANY_TIERS`/`CLIENT_TIERS` — только читались как референс. Тик
  по-прежнему раз в секунду через существующий `cityTickAccumulator`
  (`updateCityExpansion()`), никакой новой per-frame логики.
- Осознанно оставлено на следующие срезы этапа B: остальные 4 типа
  гражданских зданий (полиция/школа/детсад/парк); безопасность/образование
  как отдельные статы; обратная связь `health`/`satisfaction` на налоговую
  базу/доход компании (тема этапа D) — сейчас эффект ограничен
  населением/довольством/здоровьем самого района.
- Проверено headless Chromium через Playwright (`chromium.launch` с
  `--use-gl=swiftshader`): чистый boot без консольных ошибок; постройка
  поликлиники в `industrial` (без жилья) → `health` и `satisfaction` района
  заметно растут за несколько тиков, `population` остаётся 0 (ожидаемо);
  контролируемое сравнение скорости роста населения — жильё построено
  одновременно в `business` (без поликлиники) и `waterfront` (с
  поликлиникой), за одинаковое окно времени `waterfront` вырос на 13.5,
  `business` — на 9 (ровно соотношение 1.5, как и запрограммировано);
  численная проверка клирэнса (`THREE.Box3` + углы + центр через
  `distToNearestRoadSegment()`) для построенных поликлиник — минимум ~21-24
  (порог 2.6); численная проверка непересечения с соседними
  жилыми/гражданскими зданиями — минимум ~7.3 (порог 6.0); сконструирован
  вручную сейв `saveVersion: 21` (снимок сразу после среза 2 этапа A) —
  загружается без ошибок, `civic` мигрирует в дефолт для всех 4 районов,
  `health` — дефолт 50, старые данные (`money`, `housing`,
  `districtStats.population`) сохранены нетронутыми; панели
  fleet/base/tenders/contracts/systems/city/operations/fleetOperations/staff
  по-прежнему рендерятся без ошибок и на свежем буте, и после миграции
  старого сейва.

## 2026-09-20 — Этап B, срез 2: «городская инфраструктура» — второе гражданское здание (полицейский участок)

- **Второе гражданское здание этапа B**: `POLICE_BUILDING` («Полицейский
  участок» / Police Station, `🚓`), тот же конвейер строительства, что у
  поликлиники (`spawnConstructionSite()`, кольцевой перебор позиций,
  `distToNearestRoadSegment()`, `occupiedBuildingPositions()`), стоит
  950 💰 + 3 🪵 + 2 🧱, 5.5с стройки. Строится в любом из 4 районов, включая
  `industrial` (`POLICE_DISTRICT_IDS` = все районы `CITY_DISTRICTS`, тот же
  принцип, что у `CIVIC_DISTRICT_IDS`). **Визуально отличим от обоих других
  типов зданий**: архетип `tower` (компактная многоярусная муниципальная
  башня) вместо `shop` у поликлиники и `office` у жилья, тёмно-синий цвет
  `0x2c4870` вместо кремового `0xf1ece2` и терракотового `0xdba86c` — узнаётся
  в 3D-мире с одного взгляда.
- **Реальный игровой эффект**: новый стат `cityState.districtStats[id].safety`
  (0-100), считается в той же `updateDistrictStats()` по образцу `health` —
  база от уровня района + 42 к цели при построенном участке, лерп 0.2/тик.
  Участок также поднимает цель `satisfaction` (+14, независимое от клиники
  число). Бонус к скорости заселения района обобщён с «поликлиника → ×1.5» на
  «любое построенное гражданское здание в районе даёт +0.5x», и **со второй
  постройкой в одном районе складывается аддитивно**:
  `growthMult = 1 + 0.5×hasClinic + 0.5×hasPolice` — один участок без
  поликлиники даёт те же ×1.5, что одна поликлиника, а оба вместе — ×2.0.
  Видно в карточке района панели «Город и развитие»
  (`👥 N · Довольство X% · Здоровье Y% · Безопасность Z%`) и в подписи
  здания на карте.
- **Размещение**: `policeBuildingPosition(districtId)` — тот же кольцевой
  перебор, что у жилья/поликлиники, с третьим угловым сдвигом (не совпадает с
  их точками-кандидатами). `occupiedBuildingPositions()` обобщена вторым
  необязательным параметром (`excludePoliceDistrict`) и теперь объединяет
  позиции жилых, гражданских (поликлиника) И полицейских зданий — участок не
  встаёт поверх любого из них, и наоборот.
- `cityState.police[id] = { stationBuilt, pendingUntil, pos }` — новый
  верхнеуровневый объект по образцу `cityState.civic`, а не второе поле
  внутри `civic` (разные типы зданий держат независимый таймер стройки и
  позицию в отдельных объектах, тот же принцип, что и `housing`/`civic`).
- `saveVersion` 22 → 23. В `load()` добавлена миграция: `cityState.police`
  (отсутствует целиком у старых сейвов) — все 4 района получают дефолт «не
  построено»; `districtStats[id].safety` (отсутствует) — дефолт 50. Остальные
  поля (`housing`, `civic`, `population`, `districtStats.health/population`)
  сохраняются как были.
- Не тронуто: `STAGES`, `growingMeshes`, `constructionQueue`, pad-механика
  основной спирали построек, весь слой fleet/base/contracts/tenders/
  `COMPANY_TIERS`/`CLIENT_TIERS` — только читались как референс. Тик
  по-прежнему раз в секунду через существующий `cityTickAccumulator`
  (`updateCityExpansion()`), никакой новой per-frame логики.
- Осознанно оставлено на следующие срезы этапа B: школа/детсад/парк;
  образование как отдельный стат; обратная связь `safety`/`health`/
  `satisfaction` на налоговую базу/доход компании (тема этапа D).
- Проверено headless Chromium через Playwright (`chromium.launch` с
  `--use-gl=swiftshader`): чистый boot без консольных ошибок; постройка
  участка в `industrial` (без жилья) → `safety` и `satisfaction` заметно
  растут за несколько тиков, `population` остаётся 0 (ожидаемо);
  контролируемое A/B-сравнение скорости роста населения — жильё построено
  одновременно в `business` (без гражданских зданий) и `waterfront` (с
  построенным полицейским участком), за одинаковое окно времени `business`
  вырос на 7.65, `waterfront` — на 11.48 (ровно соотношение 1.500, как и
  запрограммировано); отдельная проверка по первому тику после постройки дала
  те же цифры напрямую (9 = 60×0.15 у `business`, 13.5 = 60×0.225 у
  `waterfront`); численная проверка клирэнса (`THREE.Box3` + углы + центр
  через `distToNearestRoadSegment()`) для построенных участков — минимум
  ~7.9-21 (порог 2.6); численная проверка непересечения с соседними
  жилыми/гражданскими зданиями — минимум 6.0 (порог 6.0); сконструирован
  вручную сейв `saveVersion: 22` (снимок сразу после среза 1 этапа B) —
  загружается без ошибок, `police` мигрирует в дефолт для всех 4 районов,
  `safety` — дефолт 50, старые данные (`money`, `reputation`, `housing`,
  `civic`, `districtStats.health/population`) сохранены нетронутыми;
  отдельно проверен ещё более старый сейв `saveVersion: 19` (без
  `population`/`housing`/`districtStats`/`civic`/`police` вовсе) — все поля
  получают дефолты по всей цепочке миграций без падений; панели
  fleet/base/tenders/contracts/systems/city/operations/fleetOperations/staff
  по-прежнему рендерятся без ошибок на всех трёх сценариях (свежий бут,
  миграция с 22, миграция с 19).

## 2026-09-20 — Этап B, срез 3: «городская инфраструктура» — третье гражданское здание (школа)

- **Третье гражданское здание этапа B**: `SCHOOL_BUILDING` («Школа» / School,
  `🏫`), тот же конвейер строительства, что у поликлиники и полицейского
  участка (`spawnConstructionSite()`, кольцевой перебор позиций,
  `distToNearestRoadSegment()`, `occupiedBuildingPositions()`), стоит
  920 💰 + 3 🪵 + 2 🧱, 5.5с стройки. Строится в любом из 4 районов, включая
  `industrial` (`SCHOOL_DISTRICT_IDS` = все районы `CITY_DISTRICTS`, тот же
  принцип, что у `CIVIC_DISTRICT_IDS`/`POLICE_DISTRICT_IDS`). **Визуально
  отличима от всех трёх других типов зданий**: архетип `house` (приземистый
  двухэтажный блок с боковым крылом) вместо `shop` у поликлиники, `office` у
  жилья и `tower` у участка, тёплый мягко-жёлтый цвет `0xe3b959` — узнаётся в
  3D-мире с одного взгляда.
- **Реальный игровой эффект**: новый стат `cityState.districtStats[id].education`
  (0-100), считается в той же `updateDistrictStats()` по образцу `health`/
  `safety` — база от уровня района + 42 к цели при построенной школе, лерп
  0.2/тик. Школа также поднимает цель `satisfaction` (+15, между вкладом
  клиники и участка). Аддитивная формула роста населения расширена третьим
  слагаемым: `growthMult = 1 + 0.5×hasClinic + 0.5×hasPolice + 0.5×hasSchool`
  — школа сама по себе даёт те же ×1.5, что и клиника/участок по отдельности,
  а все три здания в одном районе — ×2.5. Видно в карточке района панели
  «Город и развитие» (`👥 N · Довольство X% · Здоровье Y% · Безопасность Z% ·
  Образование W%`) и в подписи здания на карте.
- **Размещение**: `schoolBuildingPosition(districtId)` — тот же кольцевой
  перебор, что у жилья/поликлиники/участка, с четвёртым угловым сдвигом (не
  совпадает с их точками-кандидатами). `occupiedBuildingPositions()`
  обобщена третьим необязательным параметром (`excludeSchoolDistrict`) и
  теперь объединяет позиции жилых, гражданских, полицейских И школьных зданий
  — новая школа не встаёт поверх любого из них, и наоборот (существующая
  логика civic/police уже обходит школьные позиции без изменений, поскольку
  оба места вызова передают `excludeSchoolDistrict` как `undefined`, то есть
  «учитывать все школы»). Как и раньше, `residentialBuildingPosition()`
  по-прежнему не консультируется с `occupiedBuildingPositions()` вовсе — та
  же осознанная асимметрия ещё с этапа A, не в рамках этого среза.
- `cityState.school[id] = { schoolBuilt, pendingUntil, pos }` — новый
  верхнеуровневый объект по образцу `cityState.civic`/`cityState.police`, а
  не поле внутри одного из них (тот же принцип, что развёл `housing`/`civic`/
  `police`).
- `saveVersion` 23 → 24. В `load()` добавлена миграция: `cityState.school`
  (отсутствует целиком у старых сейвов) — все 4 района получают дефолт «не
  построено»; `districtStats[id].education` (отсутствует) — дефолт 50.
  Остальные поля (`housing`, `civic`, `police`, `population`,
  `districtStats.health/safety/population`) сохраняются как были.
- Не тронуто: `STAGES`, `growingMeshes`, `constructionQueue`, pad-механика
  основной спирали построек, весь слой fleet/base/contracts/tenders/
  `COMPANY_TIERS`/`CLIENT_TIERS` — только читались как референс. Тик
  по-прежнему раз в секунду через существующий `cityTickAccumulator`
  (`updateCityExpansion()`), никакой новой per-frame логики.
- Осознанно оставлено на следующие срезы этапа B: детсад/парк; обратная связь
  `education`/`health`/`safety`/`satisfaction` на налоговую базу/доход
  компании (тема этапа D).
- Проверено headless Chromium через Playwright (`chromium.launch` с
  `--use-gl=swiftshader`): чистый boot без консольных ошибок; постройка школы
  сразу во всех 4 районах, включая `industrial` (без жилья) → `education` и
  `satisfaction` заметно растут за несколько тиков (education 48.15→52.72 в
  industrial за окно ожидания), `population` остаётся 0 (ожидаемо); прямая
  проверка формулы множителя через боевые функции `districtHasClinic`/
  `districtHasPolice`/`districtHasSchool` — район с клиникой+полицией даёт
  ровно `growthMult` 2.0, район с клиникой+полицией+школой — ровно 2.5
  (соотношение 1.25, как и запрограммировано); независимо реконструирован
  тик роста населения по сырым числам популяции до/после — 12.6 = 42×0.3
  (gap×0.15×2 у района без школы) и 14.0625 = 37.5×0.375 (gap×0.15×2.5 у
  района со школой), оба совпали с формулой в точности; численная проверка
  клирэнса (`THREE.Box3` + углы + центр через `distToNearestRoadSegment()`)
  для школ во всех 4 районах (построенных после жилья/клиники/участка того
  же района, как и рекомендует UI) — минимум ~13.6-34.9 (порог 2.6);
  численная проверка непересечения со всеми соседними жилыми/гражданскими/
  полицейскими/школьными зданиями — минимум ровно 6.0 (порог 6.0, впритык по
  конструкции, но не меньше — тот же паттерн, что и у полицейского участка в
  срезе 2); отдельно подтверждена и задокументирована пред-существующая (не
  новая) асимметрия — если построить школу ДО жилья в том же районе,
  `residentialBuildingPosition()` не знает о её позиции и может лечь ближе
  порога, то же самое поведение, что и у жилья относительно клиники/участка
  с этапа A; сконструирован вручную сейв `saveVersion: 23` (снимок сразу
  после среза 2 этапа B — с `civic`/`police`/`health`/`safety`, но без
  `school`/`education` вовсе) — загружается без ошибок, `school` мигрирует в
  дефолт для всех 4 районов, `education` — дефолт 50, старые данные (`money`,
  `reputation`, `housing`, `civic`, `police`,
  `districtStats.health/safety/population`) сохранены нетронутыми, сейв
  остаётся полностью играбельным (школа успешно строится и достраивается на
  мигрированном сейве); отдельно проверен ещё более старый сейв
  `saveVersion: 19` (без `population`/`housing`/`districtStats`/`civic`/
  `police`/`school` вовсе) — все поля получают дефолты по всей цепочке
  миграций без падений; двойное переключение языка ru/en (со школой уже
  построенной) пересобирает её подпись без ошибок, `renderCityPanel()`
  показывает переведённые строки школы («Школа построена»/«School built»,
  «Образование»/«Education») на обоих языках; панели fleet/base/tenders/
  contracts/systems/city/operations/fleetOperations/staff по-прежнему
  рендерятся без ошибок на всех сценариях (свежий бут, миграция с 23,
  миграция с 19).

## 2026-09-20 — Этап B, срез 4: четвёртое и последнее запланированное гражданское здание — парк

- **Парк** (`PARK_BUILDING`, id `district_park`, 🌳) — четвёртое гражданское здание этапа B,
  тот же district-reach, что у клиники/участка/школы (все 4 района, включая `industrial` —
  `PARK_DISTRICT_IDS`), тот же паттерн стоимости/размещения (`parkBuildCost()`,
  `parkBuildingPosition()` — кольцевой перебор с пятым угловым сдвигом + `occupiedBuildingPositions()`,
  теперь с четвёртым необязательным параметром `excludeParkDistrict`), но **намеренно другой** в двух
  местах, где это важнее всего:
  - **Визуально это не четвёртая раскрашенная коробка.** `createParkMesh()` вообще не вызывает
    `createBuildingMesh()` — вместо архетипа здания это настоящее зелёное пространство,
    собранное из уже существующих helper-функций декора (`makeTree()`, `makeBush()`,
    `makeBench()` — те же, что уже использует придорожный декор и давно существующий
    одноразовый landmark «Городской парк» в `SPECIAL_PROJECTS`, см. `createSpecialProjectLandmark()`):
    круглая лужайка на каменном основании, небольшой пруд сбоку (не по центру, в отличие от
    landmark'а), 5 деревьев разных видов/размеров по краю (`harvestableTree=false` — это
    декоративная зелень, не источник дерева), пара кустов, 2 скамейки у пруда.
  - **Стройка — не кран.** `spawnConstructionSite()` (кран/леса/экскаватор) не переиспользован —
    выглядел бы абсурдно для озеленения лужайки. Вместо этого — собственная, намного более простая
    анимация «озеленения» (`spawnParkSite()`/`updateParkSites()`, отдельный массив `parkSites`,
    не связан с `constructionSites`/`growingMeshes`): вспаханная земля под колышками-ограждением
    заполняется квадратами дёрна по мере расхода уменьшающегося штабеля рулонов газона, без крана,
    стен и крыши.
  - **Не примкнута id `'park'`.** `SPECIAL_PROJECTS` уже использует этот id для несвязанного
    одноразового landmark'а с фиксированной позицией — разные системы, коллизии состояния нет
    (`cityState.park` vs `specialProjectState`), но `district_park` оставлен явным для ясности при
    grep'е.
  - **Реальный игровой эффект — намеренно НЕ четвёртый `districtStats`-стат и НЕ четвёртое
    слагаемое в `growthMult`.** Три почти одинаковых 0-100 стата подряд (`health`/`safety`/
    `education`) — ровно тот риск «систем ради количества», о котором предупреждала задача.
    Вместо клонирования паттерна парк напрямую и намного сильнее поднимает уже существующий
    `satisfaction`: `if (hasPark) target += 30` в `updateDistrictStats()` — заметно больше, чем
    у клиники (+16), участка (+14) или школы (+15). И **парк осознанно не участвует в
    `growthMult`** (нет `hasPark`-слагаемого рядом с `hasClinic`/`hasPolice`/`hasSchool`) — то есть
    парк не ускоряет заселение района, а напрямую поднимает качество жизни, что и есть его
    единственный смысл. Это genuine другой рычаг экономики города, а не четвёртая копия «построил
    — получил +X к прогресс-бару».
  - **Стоимость** — 850 💰 + 5 🪵 + 1 🧱 (0 🔩), 4.5с озеленения. Дешевле и куда менее
    ресурсоёмко по бетону/металлу, чем клиника/участок/школа (900-950💰 + 3🪵 + 2🧱) — осознанное
    решение: это ландшафтные работы, а не капитальное строительство.
  - `cityState.park[id] = { parkBuilt, pendingUntil, pos }` — новый верхнеуровневый объект по
    образцу `civic`/`police`/`school`. Никакой новой миграции `districtStats` не нужно — парк
    переиспользует существующее поле `satisfaction`.
  - **Найдена и исправлена тонкая гонка состояний**, обнаруженная при headless-тестировании под
    сильным троттлингом `requestAnimationFrame`: визуальный прогресс озеленения (`parkSites`,
    dt-суммирование за кадр в `animate()`) и проверка `Date.now()>=pendingUntil` (по реальному
    времени, в `updateCityExpansion()`) обычно завершаются практически одновременно при нормальном
    FPS, но при экстремальном троттлинге разъезжаются — итоговый парк мог появиться на сцене, пока
    старый rig озеленения с подписью «ОЗЕЛЕНЕНИЕ NN%» ещё не убрал сам себя, и они читались как
    наложенные друг на друга подписи. Добавлена защитная очистка: `parkSiteByDistrict` (Map,
    не сериализуется, не часть `cityState`) хранит ссылку на активный `parkSite` района, и в
    момент реального завершения (`updateCityExpansion()`) он принудительно убирается со сцены —
    сильнее защита, чем у клиники/участка/школы (у них та же самая теоретическая гонка существует
    нетронутой, но не мешает при обычном некликлограниченном FPS и вне рамок этого среза).
  - Не тронуто: `STAGES`, `growingMeshes`, `constructionQueue`, pad-механика основной спирали
    построек, весь слой fleet/base/contracts/tenders/`COMPANY_TIERS`/`CLIENT_TIERS` — только
    читались как референс. Тик по-прежнему раз в секунду через существующий
    `cityTickAccumulator` (`updateCityExpansion()`).
  - `saveVersion` 24 → 25. В `load()` добавлена миграция: `cityState.park` (отсутствует целиком
    у старых сейвов) — все 4 района получают дефолт «не построено». Остальные поля (`housing`,
    `civic`, `police`, `school`, `districtStats.*`) сохраняются как были.
  - Осознанно отложено на будущее: детский сад (последний тип гражданской инфраструктуры из
    исходного видения, не вошедший в этот срез — см. `TYCOON_V31_PLAN.md`); обратная связь
    `satisfaction`/`health`/`safety`/`education` на налоговую базу/доход компании (тема этапа D).
  - Проверено headless Chromium через Playwright (`chromium.launch` с `--use-gl=swiftshader`):
    чистый boot без консольных ошибок; A/B-сравнение — парк построен только в `waterfront`,
    `business` оставлен контролем без парка — за одинаковое окно ожидания `satisfaction`
    `waterfront` выросла на 20.82, `business` (контроль) — на 10.74, разница объясняется ровно
    вкладом `+30` от парка после сглаживания лерпом; прямая проверка, что `growthMult` не
    затронут — `districtHasPark('waterfront')` истинно, но формула роста населения не содержит
    `hasPark`; построены парки сразу во всех 4 районах — численная проверка клирэнса
    (`THREE.Box3` + углы + центр через `distToNearestRoadSegment()`) — минимум ~6.0-22.6
    (порог 3.2 для парка, шире, чем у остальных типов из-за большей лужайки); численная проверка
    непересечения со всеми соседними жилыми/гражданскими/полицейскими/школьными/парковыми
    зданиями — минимум ~69-72 (районы в этом тесте раскиданы далеко друг от друга, запас большой);
    сконструирован вручную сейв `saveVersion: 24` (снимок сразу после среза 3 этапа B — с
    `civic`/`police`/`school`/`health`/`safety`/`education`, но без `park` вовсе) — загружается
    без ошибок, `park` мигрирует в дефолт «не построено» для всех 4 районов, все старые данные
    (`money`, `civic`, `police`, `school`, `districtStats`, `housing`) сохранены нетронутыми, сейв
    остаётся полностью играбельным (парк успешно строится и достраивается на мигрированном сейве);
    панели fleet/base/contracts/tenders/systems/city/operations по-прежнему рендерятся без ошибок
    (свежий бут и после миграции); сделан скриншот построенного парка в 3D-мире — читается как
    настоящее зелёное пространство (лужайка, деревья, скамейки, пруд), а не здание — подтверждена
    и повторно проверена защитная очистка гонки состояний выше (после нескольких секунд ожидания
    `parkSites.length === 0`, никаких зависших подписей «ОЗЕЛЕНЕНИЕ» поверх готового парка).
